/* The replacement PPU uses one deterministic renderer for every mode.
 * This translation unit remains as an empty compatibility target for build
 * descriptions that historically named a separate pixel-at-a-time renderer. */
typedef int SnesrecompNextPpuCompatibilityTranslationUnit;
