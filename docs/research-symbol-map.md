# ActRaiser symbol map

ROM addresses and candidate names for the US game's routines and data.
Addresses are the stable identifiers; names are descriptive and may change as
a routine is better understood. See the [RAM map](ram-map.md) and
[ROM map](rom-map.md) for field layouts.

| Confidence | Meaning |
|---|---|
| Observed | A call pattern or effect was seen, but the full behavior is not mapped. |
| Mapped | Inputs, outputs, major state, and callers support a provisional name. |
| Verified | Static disassembly and runtime observations agree. |
| Stable | Verified and adopted as a public generated-code name. |

Direct-page locations are often scratch with caller-specific meaning. Do not
assume that one observed use establishes a global variable.

## Function candidates

### European-only research contracts

These are PAL ROM addresses, **not aliases for US code at the same address**.
EU English/German/French agree on the bank-00 ranges here; other PAL routines
need their own release-specific addresses.

| PAL address | Candidate symbol | Status | Contract |
| --- | --- | --- | --- |
| `$00:8D72` | `PAL_ActionPlacement_LoadModeTable` | Verified | Story/Action bank1F indices selected by title choice; starts, boxes and streamed objects |
| `$00:8EDA/$00:8FDD` | `PAL_ActionPlacement_Initial` / `PAL_ActionPlacement_Wave` | Verified | Difficulty-marker filtering for regional types; universal item parameters bypass it |
| `$00:916B` | `PAL_ActorInit_AdjustDifficultyHP` | Verified | Fragment through `$9197`, M/X0/0: flags8231 exempt; Beginner2→1HP, Expert1→2HP |
| `$00:8654/$00:86D7` | `PAL_ScoreAdd` / `PAL_Pickup_ApplyByMode` | Verified | Action score-band life awards; mode-specific pickup dispatch, separate from item placement |
| `$00:887F/$00:9925/$00:9A83` | `PAL_SpellStack_Push` / `PAL_Cast_Select` / `PAL_Cast_Complete` | Verified | Last-collected-first inventory; Action debits after effect, Story before; latter two are fragments within the player controller |
| `$00:91E0/$00:925F` | `PAL_PickupArt_Select` / `PAL_MagicIconArt_Select` | Verified | Upload-selection fragments; separate mode artwork at06:A000/A800, icon IDs at06:AC00 |
| EU `$00:D319`, DE `$D31B`, FR `$D31E` | `PAL_AitosDragon_Entry` | Verified | Type04/0D; Beginner skips projectile producer, root motion timings unchanged in tested Action encounters |
| EU `$00:D641`, DE `$D643`, FR `$D646` | `PAL_MarahnaPlant_Entry` | Verified | Type05/05; retracting head, open state80 updates; Beginner tendril24 replaces16×2 with equal48-update duration |
| `$03:A656/$03:A83A` | `PAL_Save_Write` / `PAL_Save_Load` | Verified | Nine button-only Story save/cold-Continue transactions;0200-block includes difficulty, restored before timer setup |
| `$03:B7C6/$03:C07E/$03:B3BA` | `PAL_Lair_Init` / `PAL_Town_Census` / `PAL_Master_CheckLevel` | Verified | Checked numeric behavior matches US at all three difficulties; RAM population/level/SP offsets differ |
| `$03:8271/$03:CA5E/$03:A066` | `PAL_Cycle_QueueRecovery` / `PAL_SP_Debit` / `PAL_Quake_House` | Verified | US queue formulas, price subtraction and highest-tier protection; bounded native tests, not full effect/campaign parity |
| EU `$02:C315/$02:AB5F/$02:AB9E`; DE+9, FR−14 | `PAL_Death_Route` / `PAL_GameOver_ReturnToTitle` / `PAL_Action_NewRun` | Verified |27 controlled boot traces: retry retains inventory/score/maxHP but clears sword; Game Over Start returns title; confirmed new run resets inventory and HP8 |

See the [European evidence](regional-differences-technical.md#european-difficulty-and-placement-contracts)
for timer/contact addresses, tested scope and integration boundaries.

### Boot, frame service, and common dispatch

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$00:8000` | `ResetHandler` | Stable | Reset and boot entry. |
| `$00:8325` | `SceneBootstrap_SelectActionOrWorld` | Verified | JP `$831F`. Region0/sub9 bypasses action placement and enters world init; US-only layout0900 is not loaded by this normal route. [Routing evidence](regional-differences-technical.md#unused-world-map-placement-root). |
| `$00:8520` | `NmiHandler` | Stable | Per-frame NMI service entry. |
| `$00:8525` | `IrqHandler` | Stable | IRQ entry. |
| `$00:8526/$00:852F` | `CopRequestHandler` / `BrkSoundRequestHandler` | Verified | Software-interrupt request ports; write `$035A/$035B`. |
| `$00:8418` | `WaitForVblank_Main` | Verified | Main-loop frame yield on RDNMI. HLE contract is established. |
| `$02:A85E` | `WaitForVblank_Bank02` | Verified | Bank-02 frame-yield variant. |
| `$00:8519` | `SetDataBankFromA` | Verified | Standard `PHA; PLB` helper; changes DB from A's low byte. |
| `$00:80E5` | `MainLoop_RunSimulationFrame` | Mapped | Sim-mode dispatch entry reached when map group `$18` is zero. |
| `$01:8000` | `Simulation_Interact` | Mapped | SIM interaction entry: scene 7 routes to the Palace, 8 to the temple; town input, `$7F:9750` and `$0347` gates precede `$8170`. Earlier building/icon-updater interpretation is superseded by the command-owner trace. |
| `$00:8465` | `HardwareSetup_Unknown8465` | Observed | Writes a hardware-register-style `$A1` value during the sim-dispatch tail; confirmed valid native-width code, but semantic target remains unknown. |
| `$00:8241` | `MainLoopUnknown8241` | Observed | Called twice around the `$8418` vblank wait; role remains unknown. |
| `$00:8059` | `MainLoop_Top` | Verified | Main-loop re-entry point; also the ending presenter's exit target — one of the ROM's only two RTL-long-jump destinations. |
| `$02:AC51` | `Nmi_ReadHeldButtons` | Verified | Per-frame NMI input read: `$00A0 = $4218 & $F4` (A/X/L/R held byte, masked by the input-enable byte `$F4`). `$AC4E` is an earlier branch, not this load's instruction boundary. |
| `$01:9293/$01:92AA/$02:87F3/$02:9AC4/$02:BEBF/$03:B013/$03:E535` | `WaitForVblank_SpinSites` | Verified | The complete census of RDNMI (`$4210`) busy-spin yield points beyond `$8418`/`$A85E`; now reproduced by `snesbuild poll-census --registers 4210` from rooted decoded code (including long `AF`-form reads) and mirrored in the runtime's spin whitelist. |
| `$01:93CB` | `Nmi_AckReenableBracket` | Verified | Single `LDA $004210` ack plus `$4200=#$A1` re-enable bracket; legitimately called twice per frame by sim effect paths, so it is **not** a vblank spin. |

### Action objects, sprites, and HUD

The regional magic-gesture seams are US `$00:9843` (standing dedicated-button
gate) and `$00:9A6E` (shared ground-attack prefix). Their native continuations,
release-mask mapping and CPU effects are documented in the
[gesture contract](regional-differences-technical.md#magic-gesture-integration).

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$00:8915` | `ActionObject_UpdateAll` | Mapped | Walks active `$40`-byte action-object slots and dispatches each `+$12` handler. |
| `$00:8C98` | `ActionObject_RebuildOam` | Verified | Clears OAM shadow, emits the fixed HUD, scans action objects, updates `+$30` bit `$0400`, and invokes the component emitter. The host wide replacement deliberately keeps DRAW and ACTIVATION independent: draw admission uses horizontally fitted `$22`, native vertical `$24`, and presentation margins/apron, while `$0400` uses a separately selected activation camera/window. The native pause/freeze path skips this routine even though emulated vblanks continue; a completed invocation reaches its common epilogue only after the HUD and every admitted object composition have returned. |
| `$00:8D68` | `ActionObject_EmitComposition` | Verified | Walks a 7-byte sprite composition and emits OAM components using object bank/pointer fields. Part positions use wrapping byte subtraction from the collision-header origins before the signed screen transform, followed by the emitter's one-pixel Y bias; the sword-beam rectangles from run `20260810-184935` directly confirm those semantics. |
| `$00:8E2F` / JP `$00:8E3B` | `ActionAnimation_Advance` | Native-fixture verified | Selects four-byte `{visual,delay,dx,dy}` row, mirrors velocity/extents through `+28`, writes composition `+20`/visual `+22`, returns carry at `$FF`. For selected player X=`$8A` only, flag `+30 & $8000` clears and halves fresh signed DX, rounding down; otherwise US `$F2` / JP `$F5` equal1 adds `+3E`. Terminator preserves flag. [Consumer contract](regional-differences-technical.md#shared-player-slowing-zones). |
| `$00:84EC` / JP `$00:84E6` | `Signed16_HalveFloor` | Native-fixture verified | LSR for nonnegative A; SEC/ROR for negative A. Rounds toward negative infinity, including `-1→-1`, not toward zero. Used by player slowdown. |
| `$00:8683` | `ActionAnimation_RepeatThenResume` | Mapped | Calls animation helper `$8631`, decrements object `+$38` at the sequence boundary, repeats while nonzero, then RTS-dispatches the saved `+$1E` resume. A live Bloodpool lightning actor moves from `$BD36` to this shared handler without changing generation. |
| `$00:BD36` | `BloodpoolLightning_Update` | Mapped | Primary root for the `$BD2A` vertical-lightning spawn record. Gates on object flag `$0400`, sequences authored animation/repeat commands and SFX `$10`, and later hands the same actor to `$8683`; saved resume `$BD69` executes at `$BD6A`. |
| `$00:BD90` / JP `$00:BE24` | `BloodpoolStatue_RunVolley` | Native-fixture verified | Sources US `$BD76/$BD84`, JP `$BE0A/$BE18`, types `$26/$1E`. US one shot, JP two16 frames apart, cycle137/153 active frames. Both facings/normal/Special; root activation gate, next-animation advancement on dropped allocation and paired child lifecycle verified. Freeze named family policy at initialization. |
| `$00:BDB1` / JP `$00:BE4E` | `BloodpoolStatue_TrySpawnFireball` | Native-fixture verified | Allocates after parent, retains source/facing/attack and backlink, X−16/+16/Y−8. Child entry US `$BDCB`, JP `$BE68`; allocation failure creates no actor and does not retry the shot. Legacy scratch `$1AA2` is not an eligible host actor. |
| `$00:BDF0` / JP `$00:BE8D` | `BloodpoolFireball_FlyUntilOffscreen` | Native-fixture verified | Advances through US `$8631` / JP `$8620`; the `$0400` check is reached at the two-row/eight-frame sequence boundary, not every frame, then frees via `$85B7/$85A6`. Startup has an additional pre-flight gate at `$BDDA/$BE77`. Live US flight uses state `$23`, resume `$BDD9` (target `$BDDA`), and the two visual/composition pairs in the RAM map. |
| `$00:BFDF/$00:BFF9/$00:C062` | `BloodpoolBossLightning_Create` / `BloodpoolBossLightning_Update` / `BloodpoolBossLightningFloor_Update` | Mapped | The boss allocates and links an animation-bank `$7E:5000` child. `$BFF9` selects six states: vertical and diagonal strikes at long/medium/short lengths, each alternating its bolt visual with blank `$20`; `$C062` handles the linked state-9 floor child. Run `20260810-180202` measures every live state/composition, H-flip, extent, resume, and backlink tuple in the RAM map. |
| `$00:BDFF/$00:F6E2` | `WizardBoss_SpawnRecord` / `DeathHeimWizard_SpawnRecord` | Measured | Bloodpool `$02/$08` parents and electrical children retain source `$BDFF`; Death Heim `0703` preserves the same `$BFDF/$BFF9/$C062` attack control flow and exact `$7E:5000` artwork under source `$F6E2`. Parent and child must agree on the source selected by their room. |
| `$00:BE41/$00:BE78` / JP `$00:BEDE` | `WizardBoss_SelectHealthPhase` / `WizardBoss_PostSpreadDelay` | Native normal/Special original/rematch fixtures | HP<12 selects second form at the next check. US adds31 updates after first-form state11; JP omits this delay. Shared rematch changes live in encounter animation data, not this regional policy. [Wizard contract](regional-differences-technical.md#wizard-original-fight-and-rematch). |
| `$00:E4D8` / JP `$00:E559` | `ViperBoss_SelectAttack` | Exhaustive native random-byte fixtures and booted encounters | One RNG call; US lightning on `value&3==0`, JP on `value&1==0`. Absolute X distance<48 selects close attack on the other branch. Shared rematch bolt speed/timing is independent. [Viper contract](regional-differences-technical.md#viper-attack-selection-and-rematch). |
| `$00:C1AE` / JP `$00:C245` | `PharaohBoss_RunEncounter` | Native normal/Special original/rematch fixtures | Original source `$C1A2/$C239`, rematch `$F6FA/$F779`. Shared root control flow; landing/bounce sequence40US/24JP originally,56US/24JP in rematch. Sphere allocation sets root `+$38`; child clears it after formation. Allocation failure goes directly to takeoff. [Encounter contract](regional-differences-technical.md#pharaoh-death-heim-rematch). |
| `$00:C24E` / JP `$00:C2E5` | `PharaohSphere_BecomeWallHead` | Native-fixture verified | Shared30-frame formation; flight2px/update originally,4 in rematch. Conversion at X<80 or X≥448 retains encounter source/backlink. US one arrow then withdrawal30 original/15 rematch; JP recurring136-frame arrow interval in both. Named encounter-family policy, separate from ordinary wall-head delay. |
| `$00:C2D5/$00:C2E6` / JP `$00:C36A/$00:C37B` | `PharaohArrow_Activate` / `PharaohArrow_CheckArenaBounds` | Native-fixture verified | State4 speed3 original/6 rematch; retire at unsigned X≥512 after each movement sequence. Explicit HP1/score1 and inherited attack1/2, facing, source and head backlink. US arrows outlive their retired head; retain captured generation, do not require a live parent throughout flight. |
| `$00:C8FF` / JP `$00:C990` | `KasandoraWallHead_RunFiringCycle` | Native-fixture verified | US delay immediate30 costs31 active updates. `$4000` heads cycle130US/99JP; `$5000` heads168US/137JP. Fourteen matching placements; normal/Special and allocation failure verified. |
| `$00:CB51` / JP `$00:CBD3` | `KasandoraBlueSphere_CircleAndDart` | Paired native traces | Type `$15`, nine matching placements. States `$10–$12` and601 movement samples match per trace in both regions/normal/Special; cycle177 frames. No regional speed policy justified. |
| `$00:D38E` / JP `$00:D410` | `AitosPlatformSkull_CheckProximity` | Native-fixture verified | Source `$D382/$D404`, room `$0104`. Offscreen gate then strict abs-X/Y bounds32/64US,24/24JP. Idle32 plus restart; explosion12 then no-score retirement. Normal/Special signed-axis/corner/gate fixtures and native platform traversal. Sword protection is a separate collision flag. |
| `$00:CFAA/$00:CFE3/$00:CFFE` / JP `$00:D02A/$00:D063/$00:D07E` | `AitosVolcanoFireball_Wait` / `AitosVolcanoFireball_Rise` / `AitosVolcanoFireball_Return` | Paired native traces | Source `$CF9E/$D01E`; random startup, shared16-frame rise/16-frame hold,48 eight-frame rows at4px/update upward, then ground-tested return. All six phase-aligned paths match in normal/Special. Do not translate differing initial RNG waits into a regional speed setting. |
| `$00:9CF2/$00:9D1C` | `PlayerSwordBeam_Create` / `PlayerSwordBeam_Update` | Mapped | Creates a player-linked attacker using animation `$06:8000`, then moves and retires it as a short-lived projectile. States `$13/$14` select exact visual/composition pairs `$30/$99E8` and `$31/$9A17`; both are the same six-part crescent geometry. |
| `$00:D646/$00:D785/$00:A655` | `AitosDragonBoss_Source` / `AitosDragonSwordVolley_Create` / `ActionChild_AnimateUntilOutside` | Mapped | The Aitos Act-1 dragon's source family (raw room `$0304`) owns an inactive volley controller that allocates two generic children with local counters 1/2. Loaded boss states 1/2 create the down-left/up-left priority-2 crescents measured in run `20260812-000613`; run `20260812-224123` measures their H+V-reflected rightward facing with reversed velocity and swapped extents. `$A655` repeats the selected animation until outside-activation flag `$0400` retires each child. |
| `$00:AD51/$00:B792/$00:D652/$00:E7D2` / JP `$00:ADE5/$00:B826/$00:D6D4/$00:E851` | `CentaurBoss_RunEncounter` / `BloodpoolAct1Boss_RunEncounter` / `AitosDragonBoss_RunEncounter` / `NorthwallAct1Boss_RunEncounter` | Paired native normal/Special fixtures | Reviewed programs and full animation/composition blobs match after relocation/audio accounting;401 phase-aligned samples match per region/mode pair. HP24, normal attack1/Special2. [Coverage limits](regional-differences-technical.md#first-act-boss-program-comparison). |
| `$00:C67B` / JP `$00:C70A` | `AntlionBoss_CheckEncounterTrigger` | Native boundary fixtures | Cached player X≥2432US/2304JP; latch `+$38` starts the encounter. Freeze at room entry; native actor loading/intro still apply. |
| `$00:C718/$00:C71E` / JP `$00:C7A7` | `AntlionBoss_PostVolley` / `AntlionBoss_CheckDistance` | Native signed-boundary and booted repeat fixtures | US waits36 animation frames before abs-X decision. JP decides immediately, then far branch waits61 updates before firing again. Abs-X<64 advances; equality64 stays far. [Full contract](regional-differences-technical.md#antlion-trigger-and-post-volley-decision). |
| `$00:CF3A` / JP `$00:CFBF` | `AitosBambooTrap_CheckProximity` | Native boundary and paired motion fixtures | Source `$CF2E/$CFB3`, raw room `$0104`, type06. Clear outside-activation gate and abs-X<32; no Y test. States37/38 total219 updates in both regions despite different split. JP adds one placement. [Trap contract](regional-differences-technical.md#aitos-bamboo-spike-traps). |
| `$00:AF69` / JP `$00:AFFD` | `MinotaurBoss_RunEncounter` | Native normal/Special original/rematch fixtures | Original cycles180US/147JP; both rematches117. Regional original idle48/16 plus follow-through12/11; shared rematch jump/throw acceleration. Root `+$38` latches player X before ascent. [Complete phase and encounter contract](regional-differences-technical.md#minotaur-timing-and-room-inheritance). |
| `$00:AFD2/$00:AFFA/$00:B009` / JP `$00:B066/$00:B08E/$00:B09D` | `MinotaurAxe_Create` / `MinotaurAxe_Activate` / `MinotaurAxe_SequenceBoundary` | Native-fixture verified | Original/rematch sources `$AF5D/$F6CA`, JP `$AFF1/$F749`; child retains source, facing, attack and parent, HP/score0. X offset±72US/±48JP persists in both encounters. State3 alternates spin/tiny frames; original speed3/1 versus rematch4/2. Outside-window retirement only at16-frame boundary. Full pool skips the shot. Composition pointers differ between original/rematch. |
| `$00:D974/$00:D980/$00:D9CD` | `MarahnaAct1Boss_Source` / `MarahnaAct1Boss_Init` / `MarahnaAct1Boss_WaitForPlayer` | Native normal/Special phase fixtures | JP `$00:D9F6/$00:DA02/$00:DA45`. US open-state loop versus JP opening/open/closing/protected cycle; root HP24 and Special attack1→2. Source also identifies linked body/spawner and health/flash helpers, so require root/parent identity. [Phase, geometry and collision contracts](regional-differences-technical.md#marahna-act-1-boss-vulnerability-and-placement). |
| `$00:D838` | `FlamingWheelBoss_SpawnRecord` | Measured | Aitos `$04/$07` retains `$D838` on the visible wheel body. The body is root-owned (`+$3A=0`), stays on animation `$7E:5000`, and moves among delay, repeat, and boss-AI handlers; source-plus-root ownership is therefore stable identity. Death Heim `0705` uses source `$F712` and room-owner backlink `$001C`. |
| `$00:D873/$00:D903` / JP `$00:D8F5/$00:D985` | `FlamingWheel_SelectRollOrJump` / `FlamingWheel_SpawnVolleyShot` | Native-fixture verified | Jump requires player Y<192 and abs-X<112. Five facing-dependent shots12 frames apart; no retry on pool exhaustion. Regional attack program/animation metadata match; shared rematch roll duration80→40 and shot axis speed1→4. |
| `$00:F161/$00:F2CA` | `IceDragonBoss_SpawnRecord` / `IceDragonBall_SavedResume` | Measured | Northwall `$06/$08` retains `$F161`; ice-ball children use `$8661/$F2CA`, animation `$7E:5000`, exact visuals `$12-$19` across states `$19/$1A`, and a backlink to a live same-source parent. Death Heim `0707` preserves the family under source `$F760`. |
| `$00:F16D-$F399`; `$00:F76C` | `IceDragonBoss_Family` / `IceDragonBoss_RematchEntry` | Native normal/Special, original/rematch fixtures | JP `$00:F1EC-$F418`; `$00:F7EB`. Bounded208-instruction family differs only in reviewed relocations and COP sound IDs. Root state `$12` / linked body `$11`:118 frames both originals and US rematch,106 JP rematch. All26 visual compositions match regionally; shorter rematch sequences own the delta. Root `+$3A` becomes branch0/1, not a parent pointer. [Timing and switching contract](regional-differences-technical.md#northwall-act-2-boss-original-versus-death-heim). |
| `$00:879D/$00:88D6` | `ActionItem_Apply` / `ActionHealQueue_Service` | Native pickup and leaf fixtures | JP `$00:878C/$00:88C5`. Item4 queues `floor(maxHP/4)` identically in both ROMs, item5 queues missingHP. Queue assignment replaces pending recovery; frame-phase service saturates at max. The proposed independent regional half-apple potency difference is rejected for these ROMs. [Recovery contract](regional-differences-technical.md#apples-equal-recovery-rules-different-placements). |
| `$00:A940/$00:A9BF` | `FillmoreTreeHead_Entry` / `FillmoreTreePeer_Entry` | Native normal/Special fixtures | JP `$00:A8FF/$00:A98A`. US peer is RTS; JP head `+$78` requests the adjacent peer's two-seed attack. US pre-shot animation64 frames vs JP explicit delay129 frames; two orb shots remain in both. JP seed/plant family `$00:A9F4-$AA6D`. [Family ownership and lifecycle](regional-differences-technical.md#fillmore-act-1-tree-seed-controller-and-pre-shot-wait). |
| `$00:923A/$00:9258` | `ActionHud_EmitSprites` | Verified | Emits fixed-position HUD sprites from the bank-6 HUD table; does not use camera culling. |
| `$00:9557` | `ActionObject_SpawnForMap` | Mapped | Dispatches the active map group's spawn list through the `$95DD` table. |
| `$00:853D` | `ActionObject_AllocateSlot` | Observed | Object-slot allocation scan; its `CPY #$1AA0` bound proves the object table spans 80 slots (`$06A0`-`$1AA0`), not 24. |
| `$00:9755` | `ActionArrivalCompanion_Update` | Mapped | Paired arrival actor handler. Advances/moves the orb companion, publishes motion/focus through `$7C/$7E/$80/$82`, and writes entry fade/raster controls `$CB/$CD/$CE/$CF`. Its presence in the ordinary action-object table is why activation changes must be lifecycle-aware rather than enemy-type-only. |
| `$00:97A6/$00:97C9/$00:97E4` | `PlayerArrival_Approach` / `PlayerArrival_Transform` / `PlayerArrival_Finish` | Mapped | Exact player arrival chain at object `$08A0` / handler `$08B2`. `$97A6` eventually selects the player through camera-subject pointer `$8A`; `$97C9` and `$97E4` advance the two materialization animations; completed `$97E4` installs `$9832`, flags `$0003`, and clears animation state. These three identities gate only extended horizontal activation. |
| `$00:9832` | `Player_Update` | Mapped | First normal player-control handler after `$97E4` (object base `$08A0`, handler pointer `$08B2`). It reads held buttons `$A1/$A0`; at `$9843` it tests `$A0` against `#$00C0` (A or X) and branches to the cast gate. The host arrival gate restores wide activation when this handler is installed, without changing drawing or camera presentation. |
| `$00:9DE1` | `Magic_CastGate` | Verified | Cast gate: requires `$F8==0` (no cast active), `$02AC!=0` (magic equipped), player status `$0030,X BIT #$2008` clear (not hurt/invulnerable), and MP `$21>0`. Native gate decrements only byte `$21`; `$9E0E` sets status bit `$0010`, increments `$F9`, clears input-enable `$F4` and runs the spell. Rejection resumes `$984E`. Regional pricing replaces the bounded check/debit through `$9E0D`, leaving both continuations native. Actor dispatch can enter with S below its original frame; the HLE tail inherits the generated wrapper's entry context, never current S. |
| `$00:9E89/$00:9F13` | `Magic_CreateController` / `Magic_DispatchKind` | Verified | Creates controller slot `$0860`, stores selected ID in `+38`, links it to the player, then dispatches IDs 1-4 to `$9F25/$9F71/$9FBB/$9FFA`. |
| `$00:A035` | `MagicController_WaitForCohort` | Verified | AND-scans spell slots `$06A0-$0820` once per tick; `$A054` clears the player's cast reference and frees the controller only after every cohort slot is free. |
| `$00:A061` | `Magic_InitActorAtPlayer` | Verified | Initializes a spell actor from a template and copies the current player world point from DP `$80/$82` into object `+02/+04`. |
| `$00:981C–9825` | `Player_ConsumeCheckpointRetry` | Native-prefix differential + bounded HLE | `REP #$20`, load word `$032C`; zero continues `$982F`, nonzero clears the marker and continues `$9826`. Regional Japan additionally clears score word `$1F/$20` here, corresponding to JP `$984E`, not at every death. STZ preserves A/flags; inherited actor-return context is carried through the runtime tail-transfer API. |
| `$00:84C0` | `Random_Next` | Verified | The game's PRNG. Advances the 17-byte pool `$02D0-$02E0` (carry-chain `ADC` from `$02DF` down, then a multi-byte counter increment from `$02E0` down) and returns the byte at `$02D1` in A, restoring M/X around the call. Callers typically `AND #$00FF` the result. |
| `$00:862A` | `ActionObject_SetAnimationState` | Verified | Three instructions: `STA $001A,X; STZ $001C,X; RTS` — set the object's animation state and reset its entry index to 0. Every spell stage change goes through it. |
| `$00:A0B8/$00:A0E8/$00:A188` | `MagicFire_Update` / `MagicStardust_Update` / `MagicCommon_Update` | Mapped | Fire's state/repeat handler; Stardust's randomized viewport-edge launch/collision/repeat handler — one `Random_Next` byte picks the site: `<$80` births at the TOP edge (`world_y = camera_y & $FFF8`, `world_x = camera_x + $80..$FF`), `>=$80` at the RIGHT edge (`world_x = camera_x + $100`, `world_y = (camera_y + rand-$80) & $FFF8`, i.e. screen y 0..120). The shared moving animation handler is also used by Aura and Light. |
| `$01:CFC7` | `TownActor_FetchScriptByte` | Measured | Fetches the next actor-script command from `+$16` and post-increments it. **The bank is chosen by `+$0E & $00FF`: zero reads `$7F:0000,X`, non-zero reads `$0A:0000,X`** — so class-`$01` actors run static ROM scripts while class-0 townspeople run generated RAM ones. |
| `$01:CD35` / `$01:CD6F` | `TownActor_RunScriptStep` / `kTownActorScriptCommands` | Measured | One command a frame: zero `+$1A/+$1C/+$1E`, fetch, treat `$7F` as end-of-script (`BRL $B891`), else dispatch through the 18-entry table via `PHA`/`RTS` (entries hold *target-1*) and tail-jump to `$01:AC70`. Same VM as the townspeople walk script already registered as `indirect_dispatch CD6A`. |
| `$01:CE5F` / `$01:CEAB` | `TownActorCmd_Wait` / `TownActorCmd_Land` | Measured | Cmd `$09` assembles a 16-bit operand into `+$22` and sets state 2 — the staging countdown — and leaves the cursor PAST the command, so `+$22` is the only place a wait already in progress can be read. Cmd `$0F` ends a fall, calling `$03:AF65` with 2 and passing the result + 10 to `$CF0A`. Every fireball's `$03` run terminates in `$0F`. |
| `$01:A838` | `TownScript_HouseFire` | Measured | Burning-house animation script: `$DD2D/$DD33/$DD39`, one tick each, looping. Run by world records publishing packed identity `$0A01`. |
| `$01:A853/$01:A857/$01:A85B` | `TownScript_EruptionFireballHold` / `TownScript_EruptionFireballFall` / `TownScript_EruptionGroundFire` | Measured | The volcanic eruption's three consecutive scripts. `$A853` holds `$E7D0` (climbing or staged), `$A857` holds `$E7A6` (falling), `$A85B` loops `$DD9F/$DDA5/$DDAB` four ticks a frame once landed. One record walks all three across a single flight; every one of them publishes `+$0E = $0E01`. The ground-fire frames are the burning house's own three tiles re-anchored, so the event needs no fire art of its own. Runs `20260818-070141`/`073137`/`073455`/`080109`. |
| `$00:E047/$00:E053` / JP `$00:E0DD/$00:E0E9` | `MarahnaFireball_Source` / `MarahnaFireball_Entry` | Native-fixture verified | Type05/10: shared48-update idle, abs-X/Y<80,32-update charge, four cardinal allocations,10-update burst/retire. Shared child US `$A655` / JP `$A614`. JP-only placement0505 `(39,10)`; reviewed mechanics match except US audio21 before split. Older measured US resume `$E061` is a return address, not entry. [Full contract](regional-differences-technical.md#marahna-splitting-fireballs). |
| `$00:B3BF/$00:B3CB` / JP `$00:B453/$00:B45F` | `FillmoreWallEmitter_Source` / `FillmoreWallEmitter_Entry` | Native-fixture verified | Type01/19, twelve matching0301 placements. X−8 once, outside-window gate, face player, state36 twiceUS/onceJP:360/180 active-frame firing interval. Shared rolling/falling children US `$B3F7` / JP `$B48B`; full pool skips shot. [Cadence contract](regional-differences-technical.md#fillmore-act-2-wall-emitter-cadence). |
| `$00:DE96/$00:DF3E/$00:DF63` | `MarahnaSnake_Spawn` / `MarahnaSnake_Rise` / `MarahnaSnake_Fall` | Measured | Run `20260811-232640` identifies source-`$DE96` snakes and their backlink-linked `$1D/$1E` horizontal fireballs. The parent lifecycle distinguishes these positive projectiles from source-`$E0BA` reaper orbs. |
| `$00:E0BA/$00:E13A` | `MarahnaReaper_Spawn` / `MarahnaReaperOrb_Update` | Measured | Run `20260811-232640` identifies this horizontal/aimed/vertical orb as a non-fire negative case; it must not enter the snake or `$E047` fire families despite using fire-like additive-friendly artwork. |
| `$00:E483/$00:E4D7/$00:E578/$00:E57E` | `MarahnaBoss_Spawn` / `MarahnaBossLightning_PostImpactParentResume` / `MarahnaBossLightning_BoltResume` / `MarahnaBossLightning_GroundChargeResume` | Measured | Boss source retained by parent and electrical child in map `$05/$08`; `$E578` is the diagonal descent and `$E57E` is the live post-impact floor charge. The parent uses shared repeat handler `$8683` with saved resume `$E4D7` during the latter. Complete charge/orb/bolt/ground artwork and the `$12E0` backlink contract are in `ram-map.md`. |
| `$00:F6CA/$00:F6E2/$00:F6FA/$00:F712/$00:F72A/$00:F760` | `DeathHeim_RematchSpawnRecords` | Measured | Retained `+$32` source records for rooms `0702-0707` in boss order: Minotaur, Wizard, Pharaoh, Flaming Wheel, Viper, Ice Dragon. The wrappers at `$F6D6/$F6EE/$F706/$F71E/$F736/$F76C` set room-owner backlink `$001C`, yield through `$F778`, and tail-call the original boss family. The host effect observer uses the five measured original/rematch source pairs and deliberately adds no Pharaoh accent. |
| `$00:F80F/$00:F81B` | `TanzaraBoss_SpawnRecord` / `TanzaraBoss_Spawn` | Measured | Death Heim `0708` retains source `$F80F`; `$F81B` enters the final-boss family after the same `$F778` room-owner setup. Host presentation accepts only the 50 measured `$8661`, `$7E:5000` projectile resume/state/visual/composition tuples, never the boss body or an artwork-only lookalike. |
| `$00:F8F5` / JP `$00:F974` | `TanzaraBoss_BeginSecondForm` | Controlled native defeat/transition fixtures | US clears timer stop gate `$E8`; JP preserves it. Root initially stores HP word `$8032`, then native health state settles to50. This is distinct from first-form HP24 and not evidence of a natural full victory. |
| `$00:FD25` / JP `$00:FDA2` | `TanzaraProjectile_Activate` | Native normal/Special fixtures | Explicit attack3US/4JP overrides inherited attack; state6 then34. Source is the final encounter; upper-body state48 is not part of this projectile. [Final-boss policy boundaries](regional-differences-technical.md#tanzra-forms-timer-and-projectile-strength). |
| `$02:BC82` / JP `$04:8C7D` | `ActionTimer_Service` | Native gate/BCD-boundary fixtures | M1/X0 entry; nonzero `$E8` returns without touching divider/countdown. `$E5` reload59, `$E6/$E7` saturating BCD decrement. |
| `$01:915D` | `EquipMenu_SelectMagic` | Mapped | Equip menu; writes the selected magic id to `$02AC` from the `$0299,X` HAVE flags (`AND #$7F`). |
| `$02:84E0` | `Stats_LoadWorkingMp` | Mapped | Act-entry stat staging: loads the working MP copy `$21` from persistent `$0295` (`LDA $0295; STA $21`). |
| `$00:95F0` | `ActionObject_ApplySpawnRecord` | Verified | Copies the 12-byte spawn record into the object: `+0`→`$16` anim base, `+2/+3`→`$18`/`$28`, `+4`→`$30` flags, `+6`→`$1A`, **`+7`→`$2A` ATK, `+8`→`$2C` HP, `+9`→`$2E` death score**, `+A`→`$14`. Death score uses stored tens. Also applies professional-mode (`$0349`) ATK/HP promotion and statue-drop rewrite. JP `$00:9618`: both regions rewrite statue0→2 (magic→screen clear),3→1 (sword→life) in Special; native fragments verify all eight IDs in both modes. |
| `$00:8A3C` / `$00:8AF9` | `ActionCombat_ResolveHits` | Verified | Object-vs-object damage path: victim mask `$2429` rejection, broad AABB using `$0A/$0C/$0E/$10`, then eligible attacker composition-part collision through `$8B67`. Victim bit `$0800` then deflects without HP subtraction (JP Aitos skull); otherwise `victim.$2C -= attacker.$2A`; `>0` → SFX `$02` + flash `$26=8`, `≤0` → `$00:8892` (or `$00:A54A` when flag `$4000`). JP resolver `$8A2B`, parts `$8B56`; distinct bit `$20` gates the Marahna Act-1 closed phase earlier. |
| `$00:8892` | `ActionObject_Die` | Verified | Awards `$2E` into the BCD score via `$00:873C`, installs death animation `$A382`, sets `$1A=$FF`, `$30\|=$0038`, and repeats on the next slot while `$30 & $1000` (multi-slot bosses). |
| `$00:A382` / `$00:A3E1` | `ActionObject_DeathAnim` / `ActionObject_Respawn` | Verified | At end of death animation, a nonzero `$14` re-runs the spawn from record base `$32`, arms `$26=$0258` and installs `$14` as the handler; `$A3E1` counts that down only while offscreen (`$30 & $0400`) and restarts at `$32+$0C`. `$14 == $A3E1` is the "this enemy respawns" marker. |
| `$00:879D` | `ActionItem_ApplyEffect` | Verified | Item id in `A` (the pickup object's `$38`): 0 magic+1, 1 extra life, 2 screen clear, 3 sword power-up (`$E4=$80`), 4 heal ¼ max, 5 full heal, 6 +100 stored score (1,000 displayed), ≥7 +50 stored score (500 displayed). JP `$00:878C`; source items are rewritten separately at statue initialization in Special. |
| `$00:96C3` | `ActionStatue_Break` | Verified | Handler for object type `$80` (the item statue). On `$30 & $0040` it DMAs `$06:A000 + id*$80` to VRAM `$2D80`, allocates a second slot (`$00:8538`+`$00:8581` full-record copy carries `$38`), and splits into a shatter effect (`$30=$0030`) plus the timed collectible (`$30=$0220`). |
| `$00:92CB` / `$00:941C` / `$00:94F2` | `ActionLevel_Load` / `ActionLevel_SpawnBatch` / `ActionLevel_SpawnWave` | Verified | Bank-`$0A` level stream: index `$0A:B100`, then player start, `$FF`-terminated 5-byte terrain damage boxes, then 4-byte object placements `[tileX, tileY, $38, type]` with opcodes `$FC` goto / `$FD` reserve / `$FE` checkpoint+wave gate / `$FF` end. Placements fill slots from 17 up. |
| `$00:A813` / `$00:A82D` | `ActionLevel_WaveGate` | Verified | Checkpoint object planted by the `$FE` opcode: trips on player X/Y, clears remaining objects (`$00:874E`), stores the respawn point into `$032E/$0330`, and resumes the placement stream from the cursor stashed in `$38`. |
| `$03:B7C6` / `$03:B99C` | `SimLair_InstallSeedTable` / `SimLair_SpawnMonster` | Verified | Installs ROM `$03:B825` (24 × 9 bytes, 4 lairs per town) into the `$7F:95xx/96xx` arrays; the spawner gates on population, a free world record and the respawn countdown, then writes `(cellX*16+$18, cellY*16+8)` and the monster type into records 8-11. |
| `$02:C5C9` / `$C639` / `$C66C` | `Lzss_Decompress` / `Lzss_ReadByte` / `Lzss_ReadLength` | Verified + whole-body HLE | Quintet LZSS. 256-byte ring at `$7E:2000` pre-filled with `$20`, write cursor `$AF` = `$EF`, control mask `$AE` = `$80`. **Bit-packed**: `$C639` reads 8 bits and `$C66C` a 4-bit length (len = n+2) by shifting `$B7/$B8` by the current bit phase. `src/quintet_lzss.c` is shared by both host consumers and the CPU HLE; the independent `tools/quintet_lzss.py` oracle was validated against `$7E:4000`/`$5000`/`$8000` in a live dump. |
| `$00:91C3` | `ActionTerrain_AttrAt` | Native-fixture verified | JP `$00:91CF`. Tile coords `$14`/`$16` → 4-bit quadrant-solidity attribute, via chunked map `$7E:8000` and table `$7E:05A0`; chunk columns byte `$2F`. Out of bounds: `tileX >= $84` → `$0F`, `tileY >= $86` → `$00`. All348 changed US/JP cells and32 boundary cases verified;16 native loads reproduce all eight affected maps/attribute tables. |
| `$00:93A9` / JP `$00:93D1` | `ActionLevel_ExpandDamageBoxes` | Native-fixture verified | Five-byte ROM records→count `$1AE2` and ten-byte records at `$1AE4`. Left/top padding −4/−16; width/height padding +24/+48 after scaling coordinate differences by16. All49 shared streams checked in both ROMs, including returned cursor. |
| `$00:8C12` / JP `$00:8C01` | `ActionPlayer_ContactDamageBoxes` | Native-fixture verified | Gate US `$ED` / JP `$F0`, player pointer `$8A`, flags mask `$2058`. Padded hot-point containment, top-extent-dependent height; bit80 sets flag8000, other bytes subtract HP and set hit8. Processes all overlaps in stream order, without Special promotion. [Contract](regional-differences-technical.md#terrain-and-damage-box-contracts). |
| `$02:C2E8` / JP `$04:9223` | `ActionHud_FormatScore` | Native-fragment verified | Four BCD digits at `$7F:B074`, retaining template's fifth zero at `$7F:B07C`; stored score×10 for display. Blank-fill helper US `$02:C375` / JP `$04:92B0`. Five score values × both regions verify native formatting. |
| `$00:CEF8` / JP `$00:CF7D` | `AitosMoltenRock_Entry` | Paired native/booted fixtures | Source US `$CEEC` / JP `$CF71`. Shared abs-X<128, states39/40 and same-slot reset. Odd RNG preserves facing/delay2–65; even RNG flips and waits2 (flip helper clobbers A with attributes). Eight first-flight traces across four normal/Special fixtures match;104-update first cycle is view/facing-dependent, not a universal period. |
| `$02:BAC1` | `ActionTerrain_BuildAttrTable` | Verified | Packs bit 1 of each of a metatile's four sub-tile tilemap words (`$7E:2100`, 8 bytes each) into the 4-bit mask at `$7E:05A0`; bit0 TL … bit3 BR, with `$06`/`$09` (the diagonals) meaning slopes. |
| `$02:B1F7` / `$B250` / `$B264` | `AssetScript_Run` / `AssetScript_Seek` / `AssetScript_SkipEntry` | Verified | Per-map asset-script VM; first call of action level entry. Table `$05:8000`, entries `[$18,$19,cmd…,$00]`, dispatch on the highest set bit, operand sizes 6/6/7/4/1/3/5/6 for bits 7..0. |
| `$02:B28E` | `AssetScript_LoadCharacters` | Verified + guarded whole-body HLE | Command 7. For the audited action shapes, decompresses an 8 KiB character bank or 4 KiB dialog font through `$7E:6000` and streams it to the selected VRAM word address. The read-only predicate rejects raw/non-action forms, unknown destinations, wrong CPU modes, and compressed-size mismatches so the generated body remains the exact fallback. |
| `$02:B330` | `AssetScript_LoadPalette` | Verified + guarded whole-body HLE | Command 6. Copies an audited 128-byte palette slice through `$2121/$2122`; non-action calls, unknown CGRAM destinations, and unsupported CPU modes retain the generated native body. |
| `$02:B363` | `AssetScript_LoadMetatileDefinitions` | Verified + guarded whole-body HLE | Command 5. Decompresses through `$7E:6000`, byte-swaps ROM words, and installs the complete 2 KiB table at `$7E:2100` (BG1) or `$7E:2900` (BG2). Raw, BG3, non-action, and unaudited shapes remain native. |
| `$02:B3EB` | `AssetScript_LoadMap` | Verified + guarded whole-body HLE | Command 4. Reads `[widthChunks,heightChunks,size16]`, publishes layer dimensions, and decompresses the page-major metatile-id map to the destination selected through `$46/$4A` (`$7E:8000/$C000` in stock action rooms). Raw, selector-zero, non-action, and unsupported CPU shapes remain native. |
| `$02:B4C0` | `AssetScript_ReadPointer` | Verified | Reads a 24-bit **linear** file offset from the script and converts it in place to LoROM (`bank = L>>15`, `addr = $8000\|(L&$7FFF)`) into the decompressor source `$A5-$A7`. |
| `$02:B4E8-$02:B630` | `ActionVideoConfig_Apply` | Verified + guarded whole-body HLE | Command 3 applies one 28-byte section-video record. It installs screen masks/colour math, common `$2000` priority attributes from `+4`, BGSC/mode, parallax, page animation, character animation and the timer. Action groups additionally receive BG1 `$1000` and BG2 `$0100` character-bank attributes; tilemap expansion merges the complete common word after the layer mask. Record `+27` writes `$F2`, which has no registered/recompiled direct-page reader and is renderer-nonoperative. The HLE admits the 43 audited action profile IDs and retains the generated body for non-action, unknown-profile, or wrong D/DB/M/X calls. The regional wrapper resolves initial time into `$E6/$E7` only after normal completion; `AR_ACTION_ROOM_VIDEO_HLE=0` delegates the video work without bypassing this policy. |
| `$02:B6D3-$02:B6F6` | `ActionBg_SetTileWordMasks` | Verified | Selects `$FDFF` when map group `$18` is zero or `$ECFF` for action groups, then stores the value in all three tile-word mask records `$54/$58/$5C`. |
| `$00:8433/$843E` | `SceneLoad_ForceBlankAndDisableNmi` | Verified | Waits for vblank, writes `$4200=$01` (auto-joy on, NMI off), then `$2100=$80` (forced blank). The action-entry host seam keys the exact `$843E` block and keeps display/audio advancing while the collapsed CPU-side loader interval is represented without NMI or `$0088` advancement. |
| `$03:B3D4` | `Master_LevelUp` | Verified | `$0291`+1, then max HP `$1E`+1 with hard cap `$18` (24), mirrored into Angel max/current HP `$0287`/`$0286`. Establishes max HP = `8 + (level-1)`, cap 24. |
| `$02:AB05` / `$00:8781` | `ProMode_Start` / `ProMode_AdvanceStage` | Verified | `$0349` = Professional Mode. Start sets lives 4, max HP 24, magic 0, score 0, stage `$1A/$1B=$01/$01`; act clear steps the 14-entry stage-order table `$02:9013`. Content effects: ATK/HP 1→2 (`$00:9679`/`$968C`) and statue-drop rewrite (`$00:962B`). |

### Object coroutine yields and spawn-record plumbing

The action-object engine is coroutine-based: an object's handler runs until it
calls a yield helper, which captures a continuation into an object field and
returns to the object loop. Every `JSR <helper>` return site is therefore a
dispatch entry the recompiler must register — this family caused the
action-freeze, bridge, and Death Heim bug classes.
`snesbuild rts-webs --yield-helpers --bank 00` derives the helper list from the
ROM by shape; run it after any bank00.cfg handler work.

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$00:8623/$00:8657/$00:8668/$00:8669` | `ActionObject_YieldToResume` family | Verified | Store their own JSR-return address as the object's `+$1E` resume handler and dispatch it; `$8669` additionally takes a parameter in A into field `+$38`. |
| `$00:86FA` | `ActionObject_YieldWaitFrames` | Verified | Wait-N yield: zeroes `+$06/+$08`, stores A (frame count) into `+$24`, pops the caller return and stores continuation into the `+$12` handler slot. Absent from the original folklore helper list; caused the Death Heim post-victory soft-lock. |
| `$00:A66A` | `ActionObject_YieldVariantA66A` | Mapped | Same peek-return idiom as the `$8657` family (reads the pushed return address and stores the next instruction as continuation); found by the shape census. |
| `$00:F778` | `ActionObject_YieldStashFrame` | Verified | Frame-hijacking yield: pops the caller's return frame into object field `+$3E`; `$00:F7C9` re-pushes it and `$00:F807`'s RTS consumes it. Never returns normally (exits via the yield system), so every `JSR $F778` site+3 is an RTS-dispatch entry and the paired-resume DO-NOT-REGISTER heuristic does **not** apply. |
| `$00:F8A6/$00:F8D2/$00:F977` | `ActionObject_DeepPeekHelpers` | Observed | Documented helper-shaped routines with deeper stack peeks; no JSR callers found by the census, so their dispatch role is unconfirmed. |

### Death Heim boss-rush flow

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$00:FE89` | `DeathHeim_TeleportOutSequencer` | Verified | Boss-death victory driver (object slot 50, base `$1320`); reachable only via spawn-record data words, never by code reference. Chain: `$86FA` wait 64 → `$FE9D` (player cutscene state `$0800`, beam copies player position, COP `$A0`) → `$FEE6` → wait 32 → `$FEEC`. |
| `$00:FEEC` | `DeathHeim_StageWarpToHub` | Verified | Writes rush progress `$0347 = $19 - 1`, stages the warp with a 16-bit `LDA #$0701; STA $1A`, selects current-song id `$0334=1` when `$19==8`, and deallocates the driver. `$0334` is audio state, not a separate final-boss flag. |
| `$00:F3D4` | `DeathHeim_HubStageNextBoss` | Verified | Hub map `0701` spawn-record handler (record `$F3C8` in the `$F39A` table): stages `$1A = $0347 + 2`, `$1B = $18`; branches to the all-done path when `$0347==7`. |
| `$00:A343/$00:A375` | `ActionDeparture_SelectTownOrEmergence` | Verified | Ordinary action-departure completion check, **not a post–Death Heim return**. If all six `$7F:6B18` counts equal two, set world bit 0, emergence latch `$031A`, scene 9 and song selector 4; otherwise return to the current town. Native Northwall-final and incomplete-town traces verify both branches. JP `$00:A335` only stages the town; its Palace owner later checks completion. |

### Action camera, tile streaming, and NMI graphics

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$02:B030` | `ActionCamera_ComputeTrackingDelta` | Verified | Called immediately before `$02:B091` at all three action-frame sites. Reads the selected action-object base from `$8A`, centres its `+02` X against the native 256px viewport, applies the vertical dead zone around focus Y `$82`, and writes the requested signed motion to `$7C/$7E`. It requests motion; `$02:B091` owns application, bounds, stream flags, and downstream reconciliation. |
| `$02:B091` | `ActionCamera_Update` | Verified / HLE seam | Applies camera deltas, clamps against BG dimensions, derives BG2 parallax, and raises 16-pixel strip flags. Corrected action-wide modes inset the BG1 interval by the requested finite presentation margins when the complete view fits and the canonical background plan owns a provider-backed finite BG1 canvas; fitted clamps reconcile `$7C/$7E` to actual camera motion before the original parallax/player tail. Authentic/raw, provider-off, authored-scene, and undersized-room paths retain native behavior. |
| `$02:B127` | `ActionTileStream_Dispatch` | Verified | Test-and-clears `$93` and dispatches BG1/BG2 row or column builders. |
| `$02:B158` | `ActionTileStream_BuildColumn` | Verified | Marshals one two-column upload record for the selected layer. |
| `$02:B1AF` | `ActionTileStream_BuildRow` | Verified | Marshals one two-row upload record for the selected layer; decode is page-keyed. |
| `$02:B825` | `Tilemap_DecodeColumnRecord` | Verified | Decodes level map/metatiles into a caller-supplied column record. |
| `$02:B8A0` | `Tilemap_DecodeRowRecord` | Verified | Decodes level map/metatiles into a caller-supplied row record. |
| `$02:B90D/$02:B95A` | `Tilemap_ExpandMetatileColumn` / `Tilemap_ExpandMetatileRow` | Verified + whole-body HLE | Expand 16 metatile IDs through the live definition table, word mask, and common attributes into caller-supplied column/row records. Both HLE entries share one geometry-aware wrapper, while `ActionBg_ComposeTilemapWord` is also used by the finite-world, Sky Palace, and ROM-backdrop decoders. |
| `$02:C5C9` | `Asset_DecompressLzRing` | Verified + whole-body HLE | MSB-first bitstream decoder: literal token or 256-byte sliding-window backreference. The shared core decodes the dialog-font asset at ROM `$17:ECFB` to exactly `$1000` bytes and serves both immutable host consumers and the CPU HLE. |
| `$02:B6F8-$02:B726` | `SkyPalace_SetupMetatilePage` | Mapped | Conditionally copies the Sky Palace source page at ROM `$07:D0A0` to WRAM `$7E:C200`. |
| `$02:B727` | `Tilemap_RebuildWholeMap` | Mapped | Performs whole-map record mega-bursts used by setup/UI recomposition paths. |
| `$02:BAF5` | `TileAnimation_CaptureCharacters` | Verified | When `$E1 != 0`, snapshots exactly `$1000` bytes of character VRAM beginning at word `$DA` (`$0000/$1000`) into contiguous `$7F:B800-$BFFF`. Raw cadence bit 7 is a continuation marker: clear it and retain the prior capture. Offline cumulative room assets reconstruct a byte-identical current-room source for every continuation, so arbitrary HLE loading needs no prior state. |
| `$02:BC56` | `TileAnimation_Tick` | Mapped + compatibility HLE | Advances action/sim-town tile animation and arms DMA descriptor slot 1. The recomp defers ticks during INIDISP force-blank so scene-load capture completes before animation DMA begins. |
| `$02:9204/$92D8/$931A/$9382/$93DF/$945E/$94E9/$9549/$95E9/$9665` | `ActionRaster_Build*` | Verified | Complete persistent action-room raster family: ten deterministic presets covering BG1/BG2 horizontal scroll, BG2 vertical scroll, and BG2 mosaic. A 49-room runtime census maps every user to R1-R10; the builders target HDMA channels 2/3 with tables at `$7E:6000/$6800/$7000`. |
| `$02:BC82` | `ActionTimer_Tick` | Mapped | Decrements the action timer in packed BCD once per second. |
| `$02:ABF0` | `NmiGraphics_UploadFrame` | Verified | Complete graphics uploader: OAM, scroll, tilemap records, palettes, HUD, and descriptor DMAs. |
| `$02:ACA3` | `NmiGraphics_UploadOam` | Verified | DMA of the 544-byte OAM shadow to `$2104`. |
| `$02:ACC8/$02:ACE5` | `TileUpload_DrainRecords` | Verified | Drains fixed BG1/BG2 row/column records and clears their headers. |
| `$02:ADA8` | `VramDma_Upload64Bytes` | Verified | Shared channel-1 64-byte VRAM DMA helper. |
| `$02:ADC3` | `NmiGraphics_UploadScroll` | Verified | Uploads `$22-$2D` to BG scroll registers as 10-bit values. |
| `$02:AEEB` | `NmiGraphics_UploadHudBg3` | Verified | Streams the BG3 HUD from `$7F:B000` every frame and the lower portion when `$F1` requests it. |
| `$02:BF60` | `FixedText_Compose` | Verified | Packed row/column in A, fixed record at `DB:Y`; writes BG3 text, independently of BG2 box artwork. |

### World-map construction and navigation

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$02:B475` | `WorldMap_BuildAndUpload` | Verified | Three separable phases: copy/decompress the 16 KiB base into `$7E:C000`, call `$02:865C` to stamp development, then upload all `$4000` bytes through `$2118`. Presentation does not occur inside the development builder. |
| `$02:865C` | `WorldMap_StampDevelopment` | Verified | Bounded, yield-free dynamic phase. Requires `$19=09` and destination pointer bank `$AA=7E`; consumes `$7F:9101`, the six enable words at `$7F:6B18`, town cells at `$7F:2000`, and tables `$02:8000/$8100/$87A5`; writes the developed overlay into `$7E:C000`. Production uses a pure state-free HLE; the ROM call remains only as an opt-in differential oracle. |
| `$02:86D1` | `WorldMap_TranslateTownCells` | Verified | Walks one quadrant-paged 32x32 town map and translates ordinary cell IDs through `$02:8000`; a zero translation preserves the base-map tile. |
| `$02:8726` | `WorldMap_ExpandSpecialCells` | Verified | Scans aligned even cells and expands IDs `$E3-$EF` through 13 four-byte 2x2 replacements at `$02:8100`. |
| `$02:8213` | `WorldNavigation_UpdateInputAndZoom` | Mapped | Moves focus `$0300/$0302` in two-pixel steps, stages zoom target `$0318`, and drives the scripted transition path. Stable zoom states are near `$0206`, middle `$040A`, and far `$0562`. |
| `$02:8384` | `WorldNavigation_UploadMode7Transform` | Verified | Uploads current matrix `$0304-$030A` to M7A-D and focus `$0300/$0302` to M7X/Y. The host capture carries the same values as an immutable affine scene. |
| `$02:8550` | `DeathHeim_PlayEmergence` | Verified | US-only latch-gated waits, camera shake, 64-step sprite-mask reveal and fade. Clears `$031A`, stages scene 7, returns C=1; inactive path returns C=0. Native sequence and both Palace revisits verified; not a portable JP RAM contract. |
| `$02:8601` | `DeathHeim_UpdateEmergenceShake` | Verified | Two RNG draws, masked camera offsets and inverse displacement of fixed mask record `$06D6`. Called twice per reveal step; even a zero shake mask still consumes the draws. |
| `$02:863E` | `DeathHeim_ClearRevealBit` | Verified | A8 indexes 64 rows at `$02:902F`; clear one low-byte mask bit at `$031C+offset`. Preserves A8/X, changes Y; 64 cumulative native tests cover every bit and adjacent-byte sentinels. |
| `$02:AFCB` | `NmiGraphics_UploadEmergenceMask` | Verified | Only when `$031A` is nonzero, DMA 16 bytes from `$00:031C` to VRAM word `$47F0`. Changes the repeated OBJ stencil tile, not world terrain. |
| `$01:861E` / JP `$01:85C8` | `SkyPalace_AnnounceDeathHeim` | Verified | US requires world bit 0 set/bit 1 clear; JP requires bit 1 clear and all six act counts two. Write announcement bit before dialogue (JP sets both), clear the response afterward. Native world/Palace revisits do not replay it. |
| `$01:B6CA` | `WorldNavigation_SelectLocation` | Verified | Clears `$0341`, tests focus against the seven 256x256 rectangles at `$01:B73C`, then publishes the 1-based label/destination ID. Zero therefore means outside every town border. The host adapter maps 1-7 to the existing `city.*.name` contracts; this is identity only, while OAM classification supplies replaceable glyph ownership. |
| `$02:AF86` | `NmiGraphics_UploadWorldWater` | Verified | For sim `$19=0/9`, drains `$D7-$DC` from fixed ROM bank `$0A` and performs two high-byte-only 64-byte VRAM DMAs. On world navigation the destinations replace Mode-7 tiles `$00` and `$AA`; four sources `$B000/$B040/$B080/$B0C0` advance every eight game frames. |

### Simulation camera, records, actors, and events

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$03:8053` | `Simulation_Enter` | Mapped | Sim-entry setup/graphics orchestration used by transitions and direct entry. |
| `$03:8193-$03:8237` | `Simulation_RunFrame` | Five-ROM coordinator differential + regional HLE | JP `$03:8190-$03:8242`; development divider and long-cycle threshold are captured separately from actor/recovery passes. Pending pacing activates at the next empty long-cycle/subcycle boundary; native callees remain responsible for town/event behavior. |
| `$03:8271-$03:8297` | `Simulation_QueueCycleRecovery` | US/PAL differential + regional HLE | Optional SP/HP queue replacement; native divider helper `$03:AFBD` remains responsible for quotient/remainder and hardware-register writes. Tail `$03:8298` retains all later town/event services. |
| `$01:97E5/$01:9840` | `Simulation_PlayerEffect` / `Simulation_PostedEffect` | Native bodies + scoped regional wrapper | Kind4 earthquake captures selector policy for the complete effect. Player entry M1X0, posted M0X0; PB/DB1, D0. Original JSR/JSL return frames and origin-specific resource effects stay native. |
| `$01:899B/$01:89EE/$01:8A2F` | `MasterReport_Open/ScoreBranch/Cleanup` | Native bodies + scoped regional wrapper | M1X0, PB/DB1, D0. Capture optional score-page visibility at opening; JP bypasses only the branch after native `$01:9261` input wait. Native cleanup clears objects, `$27/$29` and text; saved scores remain unchanged. |
| `$03:A066/A144/A1E8/A284/A2E3` | `Quake_House/Field/Class3/Class4/Class5Selector` | Five-ROM selector differential | M1X0, DB7F, X structure record. Only JP-selected classes replace the decision prefix; native `$03:AF65` random draw, `$03:9F05` queued actions and house `$03:B4A6` feedback remain authoritative. |
| `$01:B257-$01:B280` | `Angel_DrainRecoveryQueues` | US differential + regional HLE | Original normal-angel phase gates and native saturation helpers; tail `$01:B281` retains the dispatcher's saved-X pop and movement/arrow flow. |
| `$01:9C30`, `$01:9C34` | `Angel_RegionalRecoveryMoving`, `Angel_RegionalRecoveryStopped` | Regional HLE prefixes | JP `$01:9BFD-$01:9C26` healing is inserted after either movement-direction path. Phase uses byte `$0B04`, not the JP word overlapping the independent SP queue at `$0B05`. Native continuations `$01:9C32/$01:9C3C` restore the original flags/return frame. |
| `$01:9460-$01:948D`, `$01:948E-$01:94AF` | `Simulation_EffectService`, `Simulation_VisualService` | US-ROM coordinator differential + regional HLE | Actor/no-actor effect services hold development clocks but share the JP structure-visual divider at `$7F:7CED`; the phase is not restored on effect exit. US media helpers remain native. |
| `$03:813F` | `SimulationRecords_RestoreStagedPositions` | Verified | Copies `$130` bytes from the selected bank-7F staging buffer into live records at `$0B30,Y`; odd overlapping writes are intentional. |
| `$01:B4C6` | `SimulationCamera_Update` | Verified | Follows `$0AEE/$0AF0`, writes `$22/$24`, applies shake, and clamps the 512px town world. |
| `$01:ACD9` | `SimulationOam_Rebuild` | Verified | Clears OAM and scans 48 fixed plus 44 world records before selecting an emitter. |
| `$01:ADAD` | `SimulationOam_EmitNormal` | Verified | Emits five-byte composition parts with normal attributes. |
| `$01:AE6F` | `SimulationOam_EmitAlternate` | Verified | Same geometry as `$ADAD` with the alternate attribute transform. |
| `$01:B473` | `AngelProjectile_CheckVisible` | Verified | Tests arrow lifetime/visibility against town world and camera bounds, returning cull via carry. |
| `$01:B898` | `SimulationRecord_DispatchByType` | Verified | Per-active-record value-keyed handler dispatch; requires the full 26-entry table. |
| `$01:B8D0` | `SimulationRecord_ClassStateTable` | Verified | Record-class dispatch keyed on `+0E`; 26 classes, 133 state entries. `(class, state)` is the pair the sim3d height classifier consumes. |
| `$01:B9EC` | `BlueDragon_ClassHandler` | Verified | Class `$12`, 16 states. **State 6 is the building strike**: a 33-frame window that brackets every `$E1BD/$E209/$E255` bolt, during which the ROM drops the record onto the target and alternates bolt/flight frames each frame. |
| `$01:BE4F` | `NapperBat_ClassHandler` | Verified | Class `$13`, 16 states; state 5 covers the ground-pluck phase whose `$E71B/$E73A/$E75E` frames reach toward the ground. |
| `$01:C237` | `RedDemon_ClassHandler` | Mapped | Class `$14`; attack states 7-9 run attached-flame behaviors 18/19 (`$E340/$E35A/$E383`) and then apply the map-cell hit. |
| `$01:C4E5` | `SkullHead_ClassHandler` | Native-spawn fixture + isolated contracts | JP `$01:C46F`. Class `$15`; native spawn/target/travel/loiter/windup/post/recovery traced twice per ROM. Shared target/timer contracts do not imply identical combat, earthquake selection or whole-world timing. [State details](regional-differences-technical.md#skull-head-target-and-earthquake-state-contract). |
| `$01:C58F` | `SkullHead_SelectRoadTarget` | Native-fixture verified | JP `$01:C519`. Two `Random(8)` calls, one candidate, road bit `$0800` gate. Accept targets `(64*x+16,64*y-16)`; reject retains coordinates and returns state1. Every square/town/admission tested; no Dragon/Bat bias. |
| `$01:C63F/$01:C759` | `SkullHead_Loiter` / `SkullHead_InterruptAndResume` | Native-fixture verified | JP `$01:C5C9/$01:C6E3`. Fresh wait1800; zero is tested before decrement. Angel row/column alignment enters state `$0B` for24 calls, consuming the remaining wait with saturation, then resumes travel without resetting it. |
| `$03:F5BE` | `Town_DispatchHandlers` | Verified | Selects the active town's packed handler list and dispatches its entries. |
| `$01:AC36` | `TownProcess_AssignScript` | Mapped | Initializes a stride-12 process entry from the selected spawn-list variant. |
| `$01:AC70` | `TownProcess_RunScript` | Mapped | Advances delay/frame scripts and handles hide, loop, and terminator opcodes. |
| `$01:CFF2` | `TownProcess_AssignFromPackedSelection` | Mapped | Splits A into the list/sub-variant selectors and calls `$AC36`. |
| `$01:B778/$01:B7A8` | `SimulationEffect_Allocate` / `SimulationEffect_AllocateDedicated` | Mapped | Allocates the first free effect world record or the dedicated `$1062` record, copies pending pixel X/Y, assigns class/type, and initializes its animation/behavior. Lightning/Rain use primary class 2/3 plus dedicated class 8. |
| `$01:AA56` | `TownProcess_RestoreAndAssignAll` | Mapped | Restores staged positions and walks town process entries to assign scripts. |
| `$01:8029/$01:B1C7` | `TownProcess_Initialize` | Mapped | Town-init wrapper that assigns and begins a process script. |
| `$01:B52F` | `TownProcess_SelectVariantAll` | Mapped | Switches all town processes to the requested variant. |
| `$01:B6AE/$03:CFB3` | `TownProcess_HideAll` | Mapped | Hide sweeps used during town state changes and exit. |
| `$01:D072` | `TownActor_Spawn` | Mapped | Initializes a stride-38 actor record from its type's behavior-script table. |
| `$01:D04E-$01:D062` | `TownActor_DispatchBehavior` | Verified | Y-table RTS dispatch keyed by the record's masked selector. |
| `$01:D063` | `TownActor_MarkInitialized` | Verified | Sets bit 15 in record `+$12`; standard one-time initialization latch. |
| `$01:D08F` | `TownActor_RunScript` | Mapped | Advances an actor behavior script, including its countdown and terminator. |
| `$01:CD0C` | `TownActor_DispatchBehaviorState` | Verified | Per-frame actor behavior-state dispatch (`LDY #$CD12; BRL $D04E`) keyed by record `+$12`: state 0 = paced delay (`$CD22`), 1 = walk-script executor (`$CD35`), 3 = paced walk (`$CEFA`), 7 = `$CFAA`. |
| `$01:CD35` | `TownActor_RunWalkScript` | Verified | Walk-script executor: reads the next script byte via `$CFC7`; non-`$7F` bytes dispatch as commands through the `$CD6F` table (PHA/RTS at `$CD6B`). Command byte 3's handler `$CDCC` advances the state machine to paced walk — the step whose missing dispatch caused the 17×-fast "invisible people" bug. |
| `$01:CFC7` | `TownActorScript_ReadAdvance` | Mapped | Advances the actor's `$7F`-bank walk/animation script pointer and returns the next script byte in A. |
| `$01:9CD6` | `Reward_GrantMagicScroll` | Verified | Scroll-reward handler behind `$9C6F`: increments persistent MP `$0295` (long addressing) and the working copy `$21`, then posts the message. |
| `$03:9390/$03:944B/$03:9505/$03:95B3` | `TownDev_CycleHandlers` | Mapped | Development-cycle (hourglass) phase handlers, installed via the RAM handler pointer at `$7C45/$7C47` — invisible to static call-graph scans. |
| `$03:9DE4/$03:9E5A` | `TownDev_BuildStepDispatch` | Mapped | Build-step web: seven outer handlers funnel through `$9ED3` into the per-building-type RTS table at `$9EF3` (49 targets `$A004`-`$A4B8`), with stateful mid-loop continuations `$9EF4` → `$9E32/$9EC5` (rts_dispatch class, never plain-func). |
| `$03:9D9F` | `StructRecord_Alloc` | Verified | First-free scan over the current town's 128 × 4-byte structure-record array (base `word[$03:DC74+town*2]`); writes `{$7C9D,$7C9F,$7CA1\|$80,0}`, slot in `$7C05`, X = record; carry set = full. This is a record limit, not a direct population cap. HLE-replaced (with the `fix_bridge_limit` extension) by `ActRaiser_AllocStructureRecord`. |
| `$03:B274` | `Miracle_ApplyStructureDamage` | Verified | JSL from the `$01:9840` effect driver (4 sites) + scripted `$03:E7A6`: queues action `$90EB+1` on every active record in the aimed square (whole map for kind ≥ 4), then runs `$9E5A` twice. Lightning = kind 1 = action 2; **Wind = kind 5 = action 6**, and kind ≥ 4 takes the whole-map branch, so Wind reaches every windmill in the town (measured: `$90EB=$0005` in `runs/20260817-201256/snapshots/snap_01_gf1431`). The withdrawn bridge-destruction experiment is gone; this routine is fully native. |
| `$03:9F05` | `StructRecord_QueueAction` | Verified | `rec[3] = (rec[3] & $70) \| A` — stages a pending action for the per-type action dispatch. |
| `$03:C07E` | `Town_PopulationCensus` | Verified | Recomputes `$7E:021C+2N` (population from standing houses) and `$7F:6B26+2N` (support capacity: 32/48/72 per completed support structure, bridges 32) from the record array. HLE preserves the native contract and appends validated sidecar bridges at +32 support each. |
| `$03:95B3` | `Town_TryAllocateHouse` | Native-fixture verified | JP `$03:939E`. Requires a pending budget and old population ≤ support+2, then allocates a whole house; no final-population clamp. Failure sets status `$0010` without spending the budget. |
| `$03:8E10` | `Town_RefreshPopulationTotal` | Native-fixture verified | JP `$03:8CFD`. Copies selected town population to `$021A` and sums six populations into `$0218` (JP `$0219/$0217`); does not aggregate support. |
| `$03:B3BA/$03:E414` | `Master_TryAwardOneLevel` / `Master_ProcessLevelAwards` | Leaf and no-award wrapper native fixtures; positive wrapper code-mapped | JP `$03:B18B/$03:DF19`. Leaf awards once, never demotes. Wrapper loops with sound/dialogue, publishes next threshold, refills SP after awards and handles maximum level17. Preserve wrapper ownership when changing requirements; see [switching boundaries](regional-differences-technical.md#population-switching-established-boundaries). |
| `$03:B4A6/$03:B50F/$03:B54E` | `Lair_HouseFeedback` / `Lair_AdjustFromScore` / `Town_CreditGrowthWithCarry` | Native-fixture verified | House destruction distributes4/6/8 stock units (JP4) across unsealed lairs, restarting at the first slot for each house, or credits growth if all sealed. All16 Skull-origin seal masks verified. Score helper adds `A >> 2` to each stock; JP subtracts instead. Growth leaf inherits incoming carry. See [regional reference](regional-differences-technical.md#lair-stock-is-not-monotonic) before replacing these with host arithmetic. |
| `$03:D095/$03:D0D4` | `Town_ApplyActionScore` / `Town_ConvertActionScore` | Mapped + native conversion fixtures | Completed-act count selects stock versus growth; US converter returns `2*floor(score/10)`, unlike JP's thresholded converter. Score is BCD; output binary. |
| `$03:BA42/$03:BADD/$03:B692` | `Lair_ApplyMiracle` / `Lair_RecordDefeat` / `Lair_CheckAllSealed` | Native leaves + mapped seal check | Miracle depletion is gated separately from kills and credits growth with the SBC carry. All-sealed reads imagery/state flags, not remaining stocks. JP addresses `$03:B7CB/$03:B866/$03:B41B`. |
| `$03:B5BD/$03:B794/$03:B561` | `Lair_GuidanceSeal` / `Lair_FindGuidanceTarget` / `Lair_SettleSealGrowth` | Booted-fixture verified | Guidance requires population >=10, not exhausted stock. Seal settlement credits remaining stock without clearing it; live monster survives. JP `$03:B349/$03:B51D/$03:B2ED`. Seal flags/reward/stock are distinct state. |
| `$01:AFC8/$01:B06D/$01:B061-$B06C` | `Sim_ArrowContact` / `Sim_AngelContact` / `Sim_CombatTables` | Native combat + controlled booted defeat fixtures | Tables are four SP rewards, four contact damages, four accumulated-damage thresholds. Death requires damage > threshold, not >=. JP `$01:AF98/$01:B03D/$01:B031-$B03C`; see regional reference for every value. |
| `$01:B936/$01:B95A` | `Sim_SoulReturn` / `Sim_SoulCreditGrowth` | Booted-fixture verified | Class `$16` after monster death credits one growth on return, separately from stock debit, even when stock was zero or lair sealed. JP `$01:B8D8/$01:B8FC`. |
| `$03:813F/$03:8168` | `Sim_RestoreTownActors` / `Sim_CacheTownActors` | Booted-fixture verified | Restores/caches eight `$26`-byte SIM records at `$0B30`; six slices selected by `$03:8111`. JP `$03:813C/$03:8165`, table `$810E`. Pending souls survive native SRAM and reward once after reload. |
| `$03:A656/$03:A83A` | `Save_WriteNativePayload` / `Save_LoadNativePayload` | Booted-fixture verified for lairs/growth/actors | JP `$03:A42E/$03:A60A`. 24 stocks/flags/reloads/countdowns, six growth values and `$720` cached actor bytes survive Save/Continue and reset. Final checksum writes US `$A82E/$A833`, JP `$A5FE/$A603`; not a single-frame SRAM transaction. |
| `$01:9EE7/$03:B57B` | `Item_MagicSkull` / `Lair_SettleScriptedSeal` | Booted-fixture verified | JP `$01:9EBD/$03:B307`. Validates unsealed Bloodpool slot0, seals/credits stock/checks all-sealed even at population2, consumes item only on success. Cancellation/invalid targets retain it; no guidance technology/item reward bundle or SP refill. US waits90 between settlement and consumption; JP does not. |
| `$01:9754` | `Item_SelectTownTarget` | Booted Magic Skull picker + mapped arithmetic | JP `$01:96FF`. Sets `$7F:9215`; B/Y confirm/cancel clear it. Confirm publishes `((angel+8)&$FFC0)>>4` to `$7F:90E1/$90E5`. |
| `$03:811D` | `Sim_InvalidateNewTownActorCache` | Mapped native initializer | JP `$03:811A`; selected town's eight `+$10` flags become `$8000`, not a whole-record clear. Separate from cache/restore and new-monster spawning. |
| `$03:B97F` | `Sim_SpawnLairMonsters` | Booted slot-reuse + mapped gates | JP `$03:B708`. Requires eligible flags/nonzero stock/inactive actor slot and countdown. Initializes selected fields, posts SFX `$1F`; spawning does not debit stock. Soul-to-Dragon slot reuse must create a new actor-policy identity. |
| `$00:A6FD/$00:A2FE` | `Action_StageClearCard` / `Action_Departure` | Mapped US/JP caller contrast | JP card owner `$00:A6BC` applies score at `$A713` via `$03:CD3E`; US applies it later at `$A30D` via `$03:D095`. Regional completion must settle only once. |
| `$03:BF8C/$03:C037/$03:C072` | `Town_RefreshGrowthReports` / `Town_CheckFullDevelopment` / `Town_ReportPopulationThresholds` | Mapped + native report fixtures | US report calculation scans structures and six thresholds; JP `$03:BCC1` uses a different flag-based classifier. Thresholds are not a proof of attainable population maxima. |
| `$03:9975/$03:99A4` | `Bridge_BuildAtCrossing` | Mapped | Road-crossing checkers: on a river cell (probe `$97B0`) with the crossing flag clear, allocate bridge record type `$01`/`$11` and set road-map bit `$0080`/`$0100` at `$7F:6800+cell*2`. |
| `$03:A374` | `Bridge_BuildSteps` | Mapped | Bridge construction action handler (class `$7D1F`=2, variant = stage \| orientation, +8 Northwall ice) staging visual step programs via `$A4B8`. |
| `$03:A435` | `Bridge_ActionNoop` (preserving reset) | Verified | Bridge action-table rows 2-6 preserve the record but queue action 0 when town gate `$919E+N` is zero, otherwise action 1. Not literally side-effect-free: complete earthquake fixtures preserve both orientations with either gate but can differ in visual completion time. |
| `$03:A4A8/$03:A4B8` | `StructVisual_ArmRebuildProgram` / `StructVisual_ArmConstructionProgram` | Verified + whole-body HLE | Resolve class/variant through rebuild ROM `$03:D4E2` or construction ROM `$03:D4D2`, then share the `$A4C6` body that arms the record's 8-byte step-machine slot at `$7F:77E7+rec*8`. The HLEs and bridge sidecar use one resolver/armer model while the CPU wrappers preserve the gate, temporary stack bytes, decimal-mode ADC edges, DB-relative writes, A/X/Y/P, and RTS. |
| `$03:88ED` | `StructRecord_FindDestructibleInSquare` | Mapped | Rectangle search over records for the selected 4×4-cell square, explicitly skipping type-class 1 (bridges). |
| `$03:8B66` | `StructRecord_FindByTypeClass` | Verified | Finds an active record of class `$7C09` (continuation scan `$8B94`); callers search classes 0/3/5 only (people/smoke spawns `$8BC5`, story-event gates `$EF9E/$EFD3`) — nothing searches for bridges. |
| `$03:8EC1` | `Town_BuiltSquareListAppend` | Mapped | Dedup-append of the staged cell pair `$9550/$9552` to the town's 64-entry built-square list at `$7F:9250+town*0x80`. |
| `$01:9840` | `Miracle_EffectDriver` | Booted-fixture verified for Earthquake | Posted/enemy entry sets `$90F5`, not user-active `$90E9`, then runs `$9460/$99BE` until `$90F3` completion. Skull Head's quake does not charge SP or directly deplete lairs, but destroyed-house feedback still applies. JP `$01:97DF`. |
| `$01:97E5` | `Miracle_RunUserSelection` | Verified | JP `$01:9784`. Entry M=1/X=0: stores byte kind, sets `$90E9`, clears completion/actor/origin flags, initializes via `$9898`, pumps `$9460/$99BE` until complete, then cleans up before clearing `$90E9`. `$90F3` completion is earlier than wrapper completion; live Skull retries respect this distinction. |
| `$01:9460` | `Miracle_ServiceFrame` | Booted-fixture verified for Earthquake | Services input/rendering, angel/recovery, world actors and structure visuals without advancing the master development clock. JP `$01:93F5` shares `$7F:7CED` with development and only services structure visuals every fifth pass. Not equivalent to the modal-menu frame service. |
| `$01:C6F0` | `SkullHead_QuakeState` | Native-spawn trace +96 arbitration cases | State7 initializes animation/timer204; at post-decrement180, pending post/sealing cancels into state8, else user miracle/picker resets204 for retry, else posts kind4 to `$7F:96E8`. JP `$01:C67A` posts to `$96DC` with the same tested gates. Counts are eligible calls, not universal frame durations. |
| `$01:9898/$01:99BE` | `Miracle_InitializeKind` / `Miracle_UpdateKind` | Verified | Dispatch kind 1-5 to Lightning, Rain, Sunlight, Earthquake, and Wind initialization/update paths. |
| `$01:CABD/$01:CC38` | `MiracleLightning_ClassHandler` / `MiracleRain_ClassHandler` | Mapped | Class 2 traverses in, selects spawn-list 5 variant 2 for lightning, creates 16 impact children, then signals `$90F1`; class 3 traverses in, holds variant 3 rain for 60 ticks, traverses out, and signals `$90F1`. |
| `$03:9CFB/$03:9D4D` | `SimScene_MarkAndRebuildScanners` | Verified | Paired 128-record scanners in `$03:8053`: `$9CFB` dispatches `$9F1A`-`$9F73` to write `$E0-$E8` structure marks and is HLE-extended for sidecar marks; `$9D4D` dispatches the same record classes into `$A4F7` to stage and execute rebuild artwork and remains native. Their different outputs are both required: a correct bridge mark without the rebuild draw produces a black map cell. |
| `$03:9B5A/$03:9C43` | `SimScene_CopyTerrainMetatile` / `SimScene_CopyStructureMetatile` | Verified + whole-body HLE | Identical 2×2 metatile-copy leaves apart from source atlas `$7E:2100` / `$7E:3100`. Both clear attribute bit 9 and write into the quadrant-paged live town tilemap at `$7CAF/$7CB1`. Their HLE wrappers and bridge-side reconstruction share `ActRaiser_CopyTownMetatile`; the wrappers preserve decimal-mode addressing, A/X/Y/P, stack-RAM writes, DB-relative output, and RTS. |
| `$03:96EF` | `TownCell_TestTraversalBlocked` | Verified + whole-body HLE | Build-direction pathfinder predicate used by five direct JSR sites. Calls `$9710`, reads the terrain ID from `$7F:2000+cell`, rejects bit `$0200` in the metatile's `$7E:2100+id*8` top-left word, then rejects transient visited bit `$04` at `$7F:3800+cell`; Z set means available. The HLE folds the nested index call into the shared semantic calculation while preserving `$7C05`, A/X/Y, P, accumulator-width behavior, nested-JSR stack bytes, host-return state, and RTS. |
| `$03:9710` | `TownCell_MarkMapIndex` | Verified + whole-body HLE | Converts staged cell words `$7C4B/$7C4D` and doubled town index `$7BFB` into the quadrant-paged `$7F:2000` map index in A/X, leaving the pre-quadrant offset in `$7C05`. A whole-ROM scan finds 21 direct JSR sites and no entry into the body. The HLE shares `ActRaiser_CellMarkIndex` with the bridge sidecar and preserves flags, decimal mode, scratch, and RTS behavior. |
| `$03:9FCD/$03:9FE4` | `SimScene_WriteStructureMarkCell` / `SimScene_WriteStructureMarkBlock` | Verified + whole-body HLE | One-cell and 2×2-cell structure-mark writers. Both HLEs use the same named mark primitive as `$9CFB`; `$9710` maps the staged cell to `DB:$2000 + town/quadrant index`. |
| `$03:A4F7` | `StructVisual_StepProgram` | Verified | The visual step-machine interpreter, and the routine every per-class action handler tails into. Decrements slot `+0`; on zero, walks entries from slot `+2` handling `$FF` begin-loop / `$FE` end-loop / `$FD` end, and otherwise stores the duration into `+0`, the draw-list pointer word address into `+6`, the next entry into `+2`, and calls `$A591`. Early-outs entirely while gate `$7F:7BE9` is nonzero. Native; program and slot formats are in [rom-map](rom-map.md) and [ram-map](ram-map.md). |
| `$03:9EF5` / `$03:9F05` | `StructRecord_SetProgress` / `StructRecord_QueueAction` | Verified | The record `+3` field pair: `$9EF5` writes `A<<3` into bits 4-6 keeping `$8F`, `$9F05` writes `A` into the low nibble keeping `$70` (and therefore clears bit 7). Both are width-neutral `PHA`/`PLA` byte edits entered only at m=1/x=0 and both need `exit_mx_at ... 1 0`; a missing width pin at `$9EF5` prevents `$03:A1F4` from re-queuing its action and freezes windmills. |
| `$03:A1F4` | `Windmill_ActionWindReturns` | Verified | Class-3 action 6, queued by the Wind miracle (kind 5, whole map). Arms visual class 6 variant 2 through `$A4B8`, clears the record's `$40` "no wind" bit with `AND #$BF`, zeroes progress, and re-queues action 1 so the next scanner pass ticks the step machine. The requeue is the part the `$9EF5` misdecode ate. |
| `$03:A21A` | `Windmill_ArmConstructionVisual` | Verified | Class-3 construction arm: visual class 6, variant 4 when the record's `$40` bit is set and 0 when it is clear — the stopped and turning programs. Called by the class-3 action-0 handler and by the no-wind event. |
| `$03:E2BB` / `$03:EF9E` | `SimStory_StopWindmills` / `SimStory_WindEventEntry` | Verified | The Aitos "the wind has died" event. `$EF9E` gates on `$8B66` finding a class-3 record; `$E2BB` then walks all 128 records of the current town, ORs `$40` into every active class-3 record, arms `$A21A` and sets progress 4. **It stamps only the records that exist when it fires**, so a mill built during the event keeps turning — the divergence the Extras toggle covers. |
| `$03:E27B` / `$03:B25B` / `$03:E304` / `$03:E342` | `SimStory_WitherFields` / `SimStory_WitherOneField` / `SimStory_AnyFieldWithered` / `SimStory_RemarkFields` | Mapped | The class-2 counterparts of the windmill event: town-wide and per-record `$40` stamps, the "is the event still running?" query (carry set while any active class-2 record still carries `$40`), and the re-mark pass that sets the `$10` subtype and re-runs `$9CFB`. The query's shape is the ROM's own event-in-progress predicate. |
| `$03:BD55` | `House_BeginVariantBuild` | Mapped | JSL API that sets `$40` on an active class-0 record (refusing if already set), then arms visual class 0 through `$9FA5`, whose `+$10` variant differs from its base only in the finished metatile. Not currently decoded — no registered entry in `src/gen`. |
| `$03:A591` | `SimScene_ExecuteDrawList` | Verified + whole-body HLE | Interprets a ROM draw list as count + `{dx,dy,metatile}` triples and calls `$9C43` for each; `$9D4D` reaches it through the initial `$A4F7` rebuild-program step. Its HLE and bridge-side reconstruction share `ActRaiser_ExecuteTownDrawList`, while the CPU wrapper preserves pointer traversal, `$7C23`, nested-call stack writes, registers/flags, and RTS. |
| `$03:89F0` | `SimScene_FinishReconstruction` | Verified | Tiny native `LDA #$FFFF; STA $7C97; RTS` leaf immediately after `$9D4D/$BB94`. HLE preserves that contract and first replays each validated sidecar bridge's native initial draw command, avoiding any structure/step-slot injection. |
| `$03:F921` | `Miracle_DispatchStoryTriggers` | Verified | Matches miracle kind `$90EB`, town `$7BF9`, and aim square (`$90E1/$90E5 >> 2`) against 15 records; kind ≥4 bypasses location. Calls 11 handlers through target-minus-one RTS dispatch and continues scanning after each. JP `$03:F3FD`; PAL shares US entry. Not a general dialogue VM. See [five-ROM contracts](regional-differences-technical.md#miracle-triggered-story-events). |
| `$03:DFFA` / `$03:EEC8` / `$03:EF46` | `StoryEvent_Select` / `Aitos_AllMonstersEvent` / `Aitos_MountainEvent` | Verified | Aitos handler-table slots 1 and 4. `$EEC8` sets event 1 fired, then `$EED8` writes forced latch `$81` to `$7F:920E`. The `broken_text.rec` oracle sequence proves that latch can outlive event 1 until slot 4 is prerequisite/dispatched; `$DFFA` then consumes `$81` before scanning and returns slot 1 again. The opt-in host repair recognizes Aitos index 6 plus the exact slot-1-fired/slot-4-ready bitmap state and clears only `$920E`, after which the unchanged selector reaches slot 4. The behavior reproduces in the untouched US ROM and is not a localization-route or text-cache fault. |
| `$03:EEE1` / JP `$03:E9C5` | `Aitos_DyingManEvent` | Original-selector controls + twenty booted native/comparison transactions | Global26 test followed by town fired26 setter through preserved Y; repeated scene initialization interrupts the actor's two-pose program. Full timeout/message11 and early-Rain/message12 paths complete in all five ROMs. Comparison only latches global26 after first spawn; no fix implemented. [Native mismatch and live boundaries](regional-differences-technical.md#aitos-dying-man-scene-flag-mismatch). |
| `$03:EC02` / JP `$03:E6EE` | `Kasandora_Act2Event` | Original-selector gate controls | Before act completion, prerequisite8 clear selects prosperity message26 through `$9A`; its empty slot26 callback does not make the message unused. [Owner and source joins](regional-differences-technical.md#dialogue-only-slots-and-indirect-message-owners). |
| `$01:9C6F` | `Reward_Dispatch` | Mapped | Value-keyed reward grant dispatcher used by town story events. |
| `$03:F46E/$03:F479/$03:F487` | `TownLair_TestMask` / `TownLair_SetMask` / `TownLair_ClearMask` | Verified + whole-body HLE | Tests or mutates a bit in the current town's caller-selected lair mask. The three wrappers share the `$F497` resolver model. |
| `$03:F497` | `TownLair_ResolveMaskBit` | Verified + whole-body HLE | Resolves the current town and caller-selected pointer table to a DB-relative mask byte and MSB-first bit. |
| `$03:F4DF/$03:F4EA/$03:F4F8/$03:F508` | `TownGlobalFlag_Test` / `Set` / `Clear` / `ResolveBit` | Verified + whole-body HLE | Parallel test/set/clear family for the fixed `DB:$90FF` global town-event flags. It shares the same named bit-resolution and operation model as the per-town lair masks. |
| `$01:8819` | `TownEvent_Dispatch` | Mapped | Dispatches the event code in `$033E` through the `$F223` table. |
| `$03:8700-$03:8711` | `TownState_Dispatch` | Mapped | Per-town state dispatcher keyed by `$7CC9[town]`. |
| `$03:872A-$03:873B` | `TownState_Wait` | Five-ROM differential + regional HLE | JP `$03:86DA-$03:86EB`; identical leaf decrements `$7CE1[town]`, reloads from `$7CD5[town]` and increments `$7CC9[town]` on expiry. Regional adapter chooses the next reload only at expiry; current wait and other towns stay intact. |
| `$03:E865-$03:E88B` | `Fillmore_FishingProgress` | Five-ROM prefix differential + regional HLE | JP `$03:E359-$03:E37E`; original scene-init helpers remain native. Pending target applies at an eligible callback, preserving progress; native `$E88C/$E895` continuations own waiting/completion. Selector fired guard prevents repeated rewards. |
| `$03:E1D2-$03:E1EB` | `TownEventHandler_Dispatch` | Mapped | Per-town, 32-entry event-handler dispatcher with shared continuation `$E1EC`. |

### Town command navigation and offerings

USA addresses; descriptive candidates, not generated-code renames. The
[SIM menu reference](sim-menu-reference.md) contains the complete action/item
tables and caller-specific control flow. A saved JSR return on the stack is
one byte before the continuation PC used by localization routes.

| Address | Candidate symbol | Status | Contract / observation |
| --- | --- | --- | --- |
| `$01:8170` | `TownMenu_Run` | Mapped | Resets angel input/pose, caches town state in `$7F:9217`, and checks contextual lair inspection before opening the command menu. C=1 from an action redraws the retained selection; C=0 leaves the menu. |
| `$03:BB11` | `Lair_InspectAtAngel` | Mapped | Inspects a lair at the angel's position and can consume the town interaction before the menu opens; not temple detection. |
| `$01:81AC` | `TownMenu_OpeningReleasePoll` | Mapped | JSR site to `$8C43`; saved return `$81AE`, continuation `$81AF`. Its first frame service can already present menu artwork, before navigation begins. |
| `$01:8B7D` | `Menu_SelectCommand` | Mapped | X = list-node pointer, Y = label descriptor; `$0338/$033A` track list and selection. C=0/A=action on Use, C=1 on cancel. Town caller saves `$81BE` with Y=`$F34A`; shared Palace callers are separate owners. |
| `$01:81D7` | `TownMenu_DispatchAction` | Mapped | A = action ID 1–15; town owner saves return `$81C3`. Native JSR/RTS dispatch, normally M=1/X=0/DB=1; invoking an effect leaf directly bypasses command gates and cleanup. |
| `$01:8530/$853B/$854A/$8559` | `TownMenu_Master/Cities/Save/SpeedReturn` | Native wrappers + regional command adapter | Actions12–15 capture menu-return policy at dispatch. US retains close `$8CB6`, release `$9270`, C=0; JP-style return omits that suffix and sets C=1. All US report/save preparation and exact callee return PCs remain intact. Modern and Original menus share the result. |
| `$01:8290/82FB/8366/8431/83D1` | `Miracle_Lightning/Rain/Sun/Wind/Quake` | Controller HLE parity-tested | Actions 5–9 capture a campaign price before description. Preserve `$9217` town gate, unsigned SP comparison, confirmation, targeting (first three only), native `$03:CA5E` debit and `$01:97E5` effect. The latter's carry controls the native story callback. Exact call PCs remain part of text/selector ownership. |
| `$01:901C→9278` | `Dialogue_GlyphDelay` | Price presentation adapter | Saved return `$9026`: `Y-1` is the emitted literal source and `$7F:B000+X-2` its glyph byte. Price fields `$FCD5/$FD76/$FF13/$FDE9/$FE71` have widths 2/2/2/2/3. Updating the reader alone misses `$9003`'s direct word-copy loop. Regional digits preserve field width, palette, flags and caller state. |
| `$01:8C49` / `$01:B52F` | `Menu_DrawSelectionLabel` / `Menu_UpdateIconSelection` | Mapped | Resolve the selected category/action label through `$02:BF60`, then set selected/grey/hidden variants on the menu's fixed records. |
| `$01:8CF0` / `$01:8CE6` | `Inventory_SelectHeldItem` / `Inventory_SelectFromBase` | Mapped | Eight-slot selector; C=0/A=slot selects, C=1 cancels. Use Offering saves return `$84EF`, X=`$0898`, Y=`$F08C`; the caller reads the current `$02A2+slot` item before `$9C6E` dispatch. |
| `$01:8CB6` | `Menu_Close` | Mapped | Clears text, hides the 21 root records and restores screen configuration; includes frame service. |
| `$01:9270` | `Menu_WaitFaceRelease` | Mapped | Waits until `$A1 & $C0` is clear; not interchangeable with a fixed frame delay. |
| `$01:899B` / `$01:8A3F` | `Status_ShowMaster` / `Status_ShowCities` | Master native-fixture verified; Cities mapped | US Master B opens scores, Y closes; either closes scores. JP Master `$01:8978` closes on either button, without a score page. |
| `$01:8A9A` / `$01:8AF5` | `ProgressLog_Run` / `MessageSpeed_Run` | Save mapped; speed regional integration | Save/continue questions versus a captured 0–9 US/EU or 0–7 JP range. Text still uses `$8E29`; speed input polls retain returns `$8B2A/$8B31`. Only confirmation writes `$0200`; cancelling preserves an old 8/9. |
| `$01:8B18/$8B59` / JP `$01:8AAF/$8AF0` | `MessageSpeed_Position` / `MessageSpeed_MoveRight` | ROM differential + regional prefixes | Temporary cursor `$0A`; US mark base `$0B11`, JP `$0B12`. Right compares against 9/7, suppressing movement sound at the limit. Scale `$01:FA9A` owns ten cells at `$0C12`; JP uses eight centered cells. The adapter modifies only digit bytes in the synchronous `$7F:B324` shadow, not palette attributes or ROM. |
| `$01:91D3` / `$01:91E7` | `Inventory_ResolveTownBase` / `Inventory_CountItems` | Mapped | Base `$024C+9*($0341-1)`; count eight usable entries, not nine. |
| `$01:921B` / `$01:9239` | `Inventory_RemoveFirstMatchingItem` / `Inventory_SortDescending` | Removal mapped; sort native-fixture verified | Removal matches the first item ID. Sort (JP `$01:9179`) reorders eight IDs descending, zeros last, ninth byte untouched; not stable compaction. A UI must not independently remove the clicked slot. |
| `$03:F5BE` / `$03:F791` | `Town_RunMaintenance` / `Bloodpool_ReplenishCrop` | Five-ROM native fixtures | JP `$03:F09A/$03:F26D`; PAL matches US entries. Six ordered lists / 14 handler bodies compared. Bloodpool prerequisite event5 (`$910B & $04`), all eight town offerings empty, add item8; held inventory ignored. Runs at development subcycle4, not in temple receipt/menu service. [Maintenance contracts](regional-differences-technical.md#periodic-town-event-maintenance). |
| `$03:F7D1` | `Aitos_CheckCrossTownPopulationPrerequisite` | Five-ROM native fixtures | JP `$03:F2AD`; PAL same ROM entry. Global flag8 plus **Fillmore** population >=20 sets Aitos prerequisite6 and fired5. The unindexed population address is US `$021C`, JP `$021B`, PAL `$021E`; do not substitute the active town's population. Intent remains unknown. |
| `$03:E9ED` | `Bloodpool_BridgeEvent` | Five-ROM native scene transactions | JP `$03:E4E1`; PAL same ROM entry. With `$919F=0`, returns with fired2 still clear and ambient scene7 selected. Nonzero takes the fired/dispatched writes and scene-spawn path; the latter is inspected, not covered by the successful request fixtures. |
| `$03:FC4C` | `Town_ClearTerrainSquareList` | Five-ROM native scene transactions | JP `$03:F728`; PAL same ROM entry. Consumes word coordinate pairs ending in negative X, clears road-square flag `$0200`, runs native terrain work and refresh. Bloodpool maintenance's 13-square list tested through dialogue and temple return; not every caller or terrain type. |
| `$00:CE45` / `$00:B57D` | `Aitos_SharedEnemyEntry` / `BloodpoolAitos_SharedEnemyController` | Five-ROM native scene traces | Aitos placed `04/03` branches to Bloodpool `02/03`'s controller. JP `$CECA/$B611`; EU `$CB0C/$B235`, DE +2, FR +5. Ordinary `$4000` state10 has a live JP pose-order difference and common 52-update playback; not the Aitos boss's `$5000` state10. |
| `$01:8530/$853B/$854A/$8559` | `Town_RunMasterReport` / `Town_RunCitiesReport` / `Town_RunProgressLog` / `Town_RunMessageSpeed` | Native report/cancel traces verified | US closes menu/C=0; JP `$84FA/$84FF/$8504/$8509` keeps menu/C=1. Shared report owners do not determine this caller behavior. Accepted save/quit and speed-change paths are outside these traces. |
| `$01:9C6E` / `$01:9C85` | `Item_RunHandler` / `Item_UnwindTargetedHandler` | Mapped | `$9C6E` saves P before shared `$9C6F` dispatch. Ordinary handlers return through `$9C83`; targeted handlers can unwind through `$9C85`. Preserve native dialogue/effect/consumption order. |

### Dialogue and fixed-menu text

All addresses here are USA. Detailed contracts and regional cautions are in
[dialogue-system.md](dialogue-system.md); names remain candidates, not cfg renames.

| Address | Candidate symbol | Status | Contract / observation |
| --- | --- | --- | --- |
| `$01:8E29` / `$01:8FC5` | `Dialogue_Run` / `Dialogue_ReadToken` | Mapped | `DB:Y` source; reader loops internally at `$8FFF→8FC5` across dictionary words. Observe returned `A.low`, `Y-1` before caller `$8E57` dispatches a control; entry-only hooks miss it. Interpreter return acknowledges menu yield. |
| `$01:8F57-$8F5B` / `$02:C526-$C53C` | `Dialogue_InsertLiveName` / `NameEntry_Finish` | Mapped | `$06` selects WRAM `$0288` for `$01:9003`; finish terminates the name and trims native trailing spaces. The next wrapper-05 dialogue has source `$04:8EFC`, caller `$01:93B2`, context `$01:85CA`. This is an acceptance boundary even in native-only presentation; battery SRAM `$1439` may still hold an older name. See [dialogue reference](dialogue-system.md) and [save companions](save-format.md#unicode-player-names-and-emulator-interchange). |
| `$02:C0DF` | `FixedText_ReadToken` | Verified | Dictionary reader: stop before zero or after emitted space `$20`, maximum 12 glyphs, no added trailing space; unlike interactive `$01:8FC5` it does not delay glyphs |
| `$01:9032` / `$01:905B` | `Dialogue_ClearWindow` / `Dialogue_AdvanceAndScroll` | Verified | `$0200` selects clear versus retained rows at continuation |
| `$01:9099` | `Dialogue_ShowContinuation` | Verified | X is the byte offset of the native BG3 continuation cell. Switching to retail inside an adapted native continuation delegates the original body on the same JSR frame; no synthetic confirmation. |
| `$01:9261` / `$01:8C43` | `Dialogue_ConfirmInput` / `Menu_PollInputFrame` | Verified | Two `$8C43` / `BIT #$C0` loops wait for A/B release then press. Poll calls `$9284`, then loads DP `$A1`. Authored confirmations reuse the original poll body, checking source ownership between frames so removed authored-only prompts can retire. |
| `$01:9284` | `Menu_UpdateAndWaitFrame` | Verified | Calls `$0192B7` and `$01ACD9` before VBlank polling; required for animations during added pages/pauses. Not a bare host yield. |
| `$01:8C79` | `Menu_ResolveIndexedText` | Mapped | One-based selector, pointer base two bytes before first entry; retain logical slot when pointers alias |
| `$02:BF60` | `FixedText_Compose` | Verified | Packed row/column in A, record at `DB:Y`; writes BG3 staging |
| `$03:8238` / `$03:824D` | `EndingTour_CheckAndDescribe` | Mapped | Requires completed Death Heim progress `$0347 == 7`; indexes `$04:CC8A` by `$0341 * 2` and calls dialogue wrapper `$01:9314`. Live localization uses wrapper context `$03:8251`, not a separate credits interpreter. |
| `$02:97D4` | `SoundTest_RunModal` | Mapped | Dormant native menu; composes labels/counters at `$080B` through `$02:97EC`, changes scratch counters `$10/$12`, and closes with blank source `$02:9896` through `$02:985C`. Native TM changes `$04` → `$17`; no selector sprite. Localization observes these compositions without enabling a new normal-play entry. |
| `$02:C1B7` | `FixedText_EraseRecord` | Verified | Only `$00/$0D` controls; other bytes erase cell plus row above, no yield |
| `$02:C206` | `Hud_UpdateStatus` | Mapped | Non-action branch clears the 12-tile health row at `$7F:B08C` and supplies `$0286/$0287` to the bar writer |
| `$02:C375` | `Bg3_FillWords` | Verified | Fill Y words with A at `$7F:B000+X`, preserving X |
| `$02:C386` | `Hud_DrawHealthBar` | Mapped | Current/max HP in DP `$00/$02`; full tile per two units plus half-tile remainder |
| `$01:8D92` / `$01:8CA7` | `Choice_RunYesNo` / `Choice_EraseLabels` | Verified | Initially selects Yes; C=1 means Yes, C=0 means No/cancel, the opposite success polarity to command/inventory selection. Shared choice close joins at `$8DED`, reaches partial eraser via `$8CB0`. |
| `$01:8CCE` | `Menu_ClearTextRows` | Verified | Clears BG3 bytes `$B100-$B7FF`, retaining status strip |
| `$02:ABC4/$02:BA41` | `Bg3_ClearGeneralRows` | Verified | Clears BG3 bytes `$B000-$B6FF` |

### Save, title, transitions, and ending

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$02:A622` | `Title_RunContinueMenu` | Mapped + delegated campaign boundary | Continue/new-game state machine; branches on save checksum validity. Normal return binds the regional campaign after native selection/restoration/fades. `$0336 == 1` resumes the durable save; 0/2 create fresh in-memory campaign identities. The no-valid-save path also returns here with selection 0. |
| `$02:A88D` | `Save_VerifyChecksum` | Verified | ADD/XOR checksum of SRAM through `$701FEB`; returns validity via carry. |
| `$00:84F3` | `Save_AccumulateChecksum` | Verified + whole-body HLE | Bounded 4,086-word SRAM scan shared by save validation and writes. Accumulates the ADD checksum into `$14`/`X` and XOR checksum into `$16`/`Y`/`A`. The HLE reuses `Save_ComputeChecksum`, preserves the decimal-mode edge and reconstructs DP/register/P/RTL effects. |
| `$03:A656–A839` | `Save_WriteStoryImage` | Mapped + delegated save boundary | Copies town/status data into battery SRAM. Final checksum sequence: JSL `$00:84F3` at `$A827`, REP `$20`, sum store `$70:1FEC` at `$A82E`, XOR store `$70:1FEE` at `$A833`, then PLB/PLP/RTL. The adapter brackets the original generated body, preserving entry width and JSL return ownership; host persistence is deferred until normal return with a valid checksum. A checksum calculation alone is not evidence that a save has completed. |
| `$03:A83A` | `Save_RestoreStoryImage` | Mapped | Restores the stored town/status blocks to WRAM. Title confirmation `$02:A798–A79F` calls it when selection `$0336` is 1 (Continue); the other choices bypass that call. This is distinct from boot-time file loading and title-menu highlighting. |
| `$03:9156` | `ActionToSimulation_Dispatch` | Mapped | Relocates the stack and dispatches the act-to-sim transition chain. |
| `$03:AC8E` | `Transition_RunStep` | Observed | Counter-driven transition state step that calls `$97B0`; full role is not yet traced. |
| `$02:84EC` / JP `$02:84A8` | `Ending_RunWorldDeparture` | Five-ROM bounded disassembly | Gates on progress 8, explicitly plays silent music ID 21, runs departure/fade and requests credits `$0801`. PAL entry matches US; progress RAM and audio player relocate. This is a direct table request, not Palace scene selector 4. |
| `$02:AA9C` | `Ending_RunCredits` | Verified | Drives ending/credits entries, stamps the `ACT` completion marker into SRAM `$70:1FF0`, waits for Start, and RTL-jumps back to `$00:8059`. Relocates S to `$01FF` and never returns (dispatch-only class). |
| `$02:AB30` | `Ending_PresentPage` | Verified | A selects a 2048-byte map at `$7E:4000 + A*$0800`; MVN to `$7F:B000`, increment `$F1`, native fade-in and hold (except page 19). Carry clear also fades out the old page. |
| `$00:82C3` | `Ending_EnterViaRtlJump` | Verified | Mode-8 (`$18=08`) main-loop entry into the ending: `LDA #$02; PHA; LDX #$AA9B; PHX; RTL` — a cross-bank RTL long-jump to `$02:AA9C`. Whole-ROM scan confirms this and the presenter's exit are the only two RTL-jump sites. |

### Audio and SPC transport

| Address | Candidate symbol | Status | Contract / observation |
|---|---|---|---|
| `$02:9964/$02:9A56` | `Spc_UploadImageFromDp` | Verified | Two-stage SPC uploaders (HLE'd): stage 1 streams a length/target block image, stage 2 installs BRR sample chunks from the ROM pool. The 24-bit source pointer lives at DP `+$A5` (`LDA [$A5],Y`), not DP `+0`. Post-boot uploads drive the game's own resident ARAM uploader at `$0F0E`. |
| `$02:98B7` | `Audio_PlayIndexedImage` | Five-ROM bounded disassembly | Uses one-based ID: loads table `$02:C7E5[(ID-1)*3]`, records source `$AB/$AD`, uploads and starts image song 1. Ending and dormant Music Mode call it; ID 21 is silence. [Regional entries](rom-map.md#debugging-and-unassigned-routines). |
| `$02:9ACD` | `Spc_BootImageDriver` | Mapped | Boot-time upload driver; installs the sound engine at ARAM `$0400` and jumps it there. |
| `$00:A3FE-$00:A454` | `BossMusic_LoadHandshake` | Verified | Six-step blocking CPU↔SPC handshake: send `$F1` → `$A410` wait for echo → send `$01` → `$A427` wait for port clear → `$A431` send `$F0` → completion at `$A454`. Timing-sensitive; HLE'd upload plus native handshake is the working combination. |
| `$02:AC29-AC3C` | `Nmi_ForwardAudioRequests` | Verified | NMI tail: one 16-bit `LDA $035A; STZ $035A; STA $2142` forwards BOTH request ports per frame — COP event id (`$035A` → port `$2142`, low byte) and BRK SFX id (`$035B` → port `$2143`, high byte) — alternating with a zero write on odd frames (`LSR $88` carry gate). COP event ids are **audio/event commands**, not spawn requests (a proven red herring). Decoded fully 2026-07-16. |
| `$02:B63B/$02:B66C` | `Audio_ScriptSongChange` | Verified, selector clarified 2026-09-20 | `[$A2]` command reads song number, selector and source pointer. It returns if `$0334` differs from the selector; on a match it skips upload only if source `$A5/$A7` equals loaded `$AB/$AD`. Otherwise `$F0` halt → `$FF` → `JSL $9964` upload → song number to `$2140`. |
| `$03:9E6B` / `$03:9E5A` | `Audio_PostIdleSfxSlot` | Observed | The dominant `$035B` writers by volume — 732 + 22 posts of **id `$00`** in one ~13500-frame session, against 12 stray key-ons. id `$00` is the idle/clear post, not a sound; the NMI forwards zero as "nothing pending". Any SFX tooling must exclude it or it claims unrelated key-ons. |
| `$01:B936` (+`$01:B967`) | `Sfx_Request_Id08` | Observed | Posts SFX id `$08` (32 requests). Keys common-bank srcn `06` (27) / `02` (5) on voices 6-7, pitch `$0385-$3AAE` — one sample stretched ~16x, so identity is (srcn, pitch), not srcn. |
| `$01:BB28` | `Sfx_Request_Id10` | Observed | Posts SFX id `$10` (113 requests, the busiest real effect). Keys srcn `09` almost exclusively, voice 7, `volL/volR` symmetric `30-47`. |
| `$01:BC16` (+`$01:C1CB`) | `Sfx_Request_Id18` | Observed | Posts SFX id `$18` (31 requests). Widest spread: srcn `0A`/`06`/`02`/`08`/`03` across voices 6-7. **`volL/volR` spans `-71..47 / -38..71`** — asymmetric and sign-inverted, i.e. the driver already pans this effect. |
| `$03:B97F` | `Sfx_Request_Id1F` (caller alias) | Observed audio; gameplay owner mapped | `Sim_SpawnLairMonsters` above posts SFX id `$1F` at `$03:BA17` after spawning (33 requests in the audio trace). srcn `09`/`06`/`0A`. Not a standalone SFX-only routine. |
| `$01:CEE5` | `Sfx_Request_Id0C` | Observed | Posts SFX id `$0C` (7 requests). srcn `08`, voice 7, fixed pitch `$102A`, constant centered volume `47/47`. |
| `$01:BF67` | `Sfx_Request_Id1E` | Observed | Posts SFX id `$1E` (3 requests). srcn `0B`, voice 7, fixed pitch `$1123`. |
| `$01:BC06` | `Sfx_Request_Id24` | Observed | Posts SFX id `$24` (6 requests). srcn `06`/`09`, voice 7. |
| `$0334` | `Wram_CurrentSongId` | Verified | Selected/requested music identity used to choose a script declaration; loaded source is separately tracked in `$AB/$AD`. Zeroed by transition stop `$00:83F2`. |

## Data and table candidates

These are data identities rather than functions. Preserve that distinction in
the eventual symbol format.

| Address / range | Candidate symbol | Status | Meaning |
|---|---|---|---|
| `$01:A227` | `TownProcess_SpawnListTable` | Mapped | Maps family/list ID to a variant-pointer array; variant counts depend on family (menu icons use color/grey 0/1), not a universal five-entry layout. |
| `$01:D128+` | `TownUi_Compositions` | Mapped | Count plus five-byte OBJ parts, beginning with Movement's `$D128`; not six-byte actor placement records. Root-menu initialization records instead begin at `$01:AB32`. |
| `$01:AB20` / `$01:AB32` | `SimFixed_SceneInitPointers` / `TownMenu_FixedInitRecords` | Mapped | Scene pointers and six-byte X/Y/family initialization records; the six towns share `$AB32` and its first 21 menu entries. |
| `$01:F32E-$F349` | `TownMenu_SelectionList` | Mapped | Packed category/action bytes with `$FF` separators and a double terminator. |
| `$01:F34A/$F34C/$F36A` | `TownMenu_LabelDestination` / `ActionLabels` / `CategoryLabels` | Mapped | Packed destination, 15 action pointers and six category pointers; fixed-text grammar. |
| `$01:F08C/$F08E` | `HeldInventory_BaseDescriptor` / `ItemLabels` | Mapped | WRAM base `$02A2` and 20 pointer records beginning with icon-family byte; aliased text does not imply aliased behavior. |
| `$01:A2C1` / `$01:D134/$D3E6` | `ObservePeople_IconVariants` / `ObservePeople_IconColor/Grey` | Mapped | Family `$0B`; selected/grey scripts `$A3A1/$A48D`. Small angel reused by the Describe hint, not the Listen portrait. |
| `$01:D1E2/$D494` / `$01:D1F7/$D4A9` | `Choice_YesCompositions` / `Choice_NoCompositions` | Mapped | Color/grey pairs for family `$23/$24`; resolve through variant tables `$A321/$A325`. |
| `$01:E099` | `TownActor_BehaviorScriptTable` | Verified | Actor-type-indexed behavior-script pointers. |
| `$01:E7D9` | `TownActor_SpriteFrameTable` | Verified | Actor-type-indexed sprite-frame/composition pointers. |
| `$03:F5ED-$03:F620` | `Town_HandlerLists` | Verified | Six-town outer table plus packed handler lists consumed by `$F5BE`. |
| `$03:DCA2/$03:DCAE/$03:DCBA` | `Town_EventPrereqPointers` / `Town_EventFiredPointers` / `Town_EventDispatchedPointers` | Mapped | Per-town story-event bitmap pointers to `$7F:9107/$911F/$9137`; four bytes per town. These are not lair masks. See [story-event bitmaps](ram-map.md#story-event-bitmaps-7f9107-7f914e). |
| `$00:96AF-$00:F39A` | `ActionMap_SpawnTables` | Mapped | The full eight-table per-map-group battery: `$96AF/$A8F6/$B449/$C11E/$CD9B/$D928/$E722/$F39A`. Each points to per-object spawn records: base`+0x0C` = init handler (JSR-shaped), base`+0x0F` = steady-state handler, record`[0x0A]` = the **polymorphic** field-`$14` slot (handler for some object types, plain data for others — not bulk-derivable). |
| `$00:F39A+` | `DeathHeim_SpawnTable` | Verified | Distinct hub, six boss arenas, and final-boss spawn table. |
| `$01:CD12` | `TownActor_BehaviorStateTable` | Verified | Eight behavior-state handler entries consumed by `$01:CD0C`. |
| `$01:CD6F` | `TownActorScript_CommandTable` | Verified | Eighteen script-command handlers (`$CD93`-`$CEE5`) dispatched by `$CD35`'s PHA/RTS at `$CD6B`. |
| `$01:B8D0` | `SimulationRecord_TypeHandlerTable` | Verified | The 26-entry (real bound `$B904`) per-record-type table behind `$01:B898`; town actor types `0x12/0x13` sit past the old 16-entry cap. |
| `$01:9C94` | `Reward_HandlerTable` | Verified | Twenty reward-grant handler entries consumed by `$01:9C6F`'s PHX/PHA/RTS dispatch. |
| `$03:F99A` | `Miracle_StoryTriggerTable` | Verified | 15 six-byte `{miracle_kind, town, square_x, square_y, handler_minus_one:u16}` records plus `$FF` terminator, reaching 11 handlers. Same logical entries in all five ROMs; JP table `$03:F476`. The first byte is a miracle kind, not a story-event id. |
| ROM `$05:8000` | `AssetScriptTable` | Verified | Per-map presentation/asset command stream after the `"SY\0"` header. Entries are keyed by `$18/$19`; embedded 24-bit pointers are linear ROM offsets. The immutable room loader cumulatively replays entries within an act to resolve inherited background assets. File offset `$028000`. |
| ROM `$02:893E+` | `ActionVideoProfileTable` | Verified | Fixed 28-byte presentation records used by asset command 3. The stock 49-room census references 43 distinct action IDs (`$03-$2E` excluding `$08`). File offset `$01093E`. |
| ROM `$02:96D4-$97D3` | `ActionRasterWaveform` | Verified | Nominal 256-byte waveform used by persistent action raster presets. R4's original 16-bit-index quirk instead reads its settled window from adjacent `$02:97D4+phase`; do not extend this symbol across that code/data boundary. File range `$0116D4-$0117D3`. |
| `$02:C7E5` | `Song_PointerTable` | Verified | Seventeen 3-byte song-image pointers; the stage-1 upload source address is a stable track identity (more arrive via inline `[$A2]` script pointers through `$02:B4C0`). |
| ROM `$08:8000` | `BrrSampleChunkPool` | Verified | Length-prefixed `[len16][BRR data]` instrument chunks; the chunk index is a stable ROM-wide instrument ID (per-sample HD-swap seam). |
| ARAM `$0F0E/$0F12/$0F48` | `SpcResident_Uploader` | Verified | The sound engine's own resident uploader (SPC-side, not SNES bus): entry `$0F0E`, `$CC`-wait spin `$0F12`, finalize tail `$0F48` (`MOV X,#$31; MOV $F1,X; RET`). The HLE completes it by jumping `spc->pc` to the tail. |
| ROM `$07:D0A0` | `SkyPalace_MetatilePage` | Verified | 16×16 source metatile page copied by Sky Palace setup. |
| ROM `$17:ECFB` | `DialogFont_CompressedTiles` | Verified | Compressed asset whose first word declares a `$1000`-byte 256-tile 8×8 2bpp output. Font tile indices correspond to character codes; decoded by `$02:C5C9` into BG3 character space. File offset `$0BECFB`. |
| ROM `$0D:C000` | `SkyPalace_BgCharacterBank` | Verified | `$4000`-byte BG character bank, byte-identical to VRAM `$0000-$1FFF` words in both gf460/gf668 Sky Palace captures. Contains dialog-frame chars `$CE/$CF/$DE/$DF/$EE/$FF`. File offset `$06C000`. |
| ROM `$1C:BF73` | `SkyPalace_DialogPalette7` | Verified | Exact 16-color CGRAM palette used by the 8×8 dialog-frame characters. File offset `$0E3F73`; stable across ordinary dialogue and submenu captures. |
| ROM `$02:8000-$80FF` | `WorldMap_OrdinaryCellTranslation` | Verified | 256-byte town-cell to world-tile translation table; zero means preserve the base tile. File offset `$010000`. |
| ROM `$02:8100-$8133` | `WorldMap_SpecialCellExpansion` | Verified | Thirteen four-byte 2x2 replacements for aligned special cells `$E3-$EF`. File offset `$010100`. |
| ROM `$02:87A5-$87B0` | `WorldMap_TownDestinations` | Verified | Six little-endian destination offsets into the 128x128 developed tilemap. File offset `$0107A5`. |
| ROM `$01:B73C-$B757` | `WorldNavigation_LocationRegions` | Verified | Seven `(x,y)` top-left pairs for 256x256 focus rectangles: Fillmore, Bloodpool, Kasandora, Aitos, Marahna, Northwall, Death Heim. File offset `$00B73C`. |
| ROM `$06:B341-$F340` | `WorldMap_BaseTilemap` | Verified | Uncompressed row-major 128x128 Mode-7 tilemap, 16 KiB. File range `$033341-$037340`. |
| ROM `$0A:B000-$B0FF` | `WorldMap_WaterAnimationFrames` | Verified | Four 64-byte 8bpp high-byte planes used to replace Mode-7 tiles `$00` and `$AA` every eight game frames. File range `$053000-$0530FF`. |
| ROM `$0E:8000-$BFFF` | `WorldMap_Mode7Characters` | Verified | 256 uncompressed 8x8 8bpp Mode-7 tiles, 64 bytes each. File range `$070000-$073FFF`; tiles `$00/$AA` receive the animated high-byte replacements. |
| ROM `$1C:BF93-$C192` | `WorldMap_Palette` | Verified | Complete 256-entry BGR555 world palette, 512 bytes. File range `$0E3F93-$0E4192`. |

## Stable state anchors used by handwritten code

This is deliberately a short cross-reference, not a duplicate of
[ram-map.md](ram-map.md). Names here are appropriate for shared constants;
polymorphic fields and temporary direct-page values should remain local.

| WRAM | Semantic name | Meaning |
|---|---|---|
| `$7E:0018` | `MapGroup` | Non-action, six action kingdoms, Death Heim, or ending group. |
| `$7E:0019` | `CurrentMap` | Town/submode or action-map number within the group. |
| `$7E:001A/$001B` | `DestinationMap` / `DestinationMapGroup` | Pending transition destination. |
| `$7E:0022/$0024` | `Bg1CameraX/Y` | BG1 and primary camera coordinates. |
| `$7E:0026/$0028` | `Bg2CameraX/Y` | BG2/parallax coordinates. |
| `$7E:002E/$0030` | `Bg1Width/Height` | BG1 world dimensions. |
| `$7E:0032/$0034` | `Bg2Width/Height` | BG2 world dimensions. |
| `$7E:0046/$004A` | `Bg1MapBase/Bg2MapBase` | Live page-major action map bases; stock action bootstrap installs `$8000/$C000`. |
| `$7E:0052/$0056` | `Bg1MetatileBase/Bg2MetatileBase` | Live action metatile-definition bases; stock action bootstrap installs `$2100/$2900`. |
| `$7E:0054/$0058` | `Bg1TileWordMask/Bg2TileWordMask` | Per-layer masks applied before attribute merge; action rooms use `$ECFF`. |
| `$7E:006B/$006F` | `Bg1TileAttributes/Bg2TileAttributes` | High-byte attribute merges installed by the video-profile path; action rooms add `$10/$01`. |
| `$7E:0088` | `GameFrame` | NMI-maintained game-frame counter. |
| `$7E:0093` | `TileStreamRequestFlags` | BG1/BG2 row/column crossing flags. |
| `$7E:00D7-$00DD` | `AnimationDmaDescriptor1` | Mode-dependent tile-animation descriptor. World navigation leaves a `$0A:B000/B040/B080/B0C0` water source in `$D7`; towns use unrelated WRAM-backed animation data. |
| `$7E:0300/$0302` | `WorldFocusX/Y` | Canonical world point at authentic screen centre. |
| `$7E:0304-$0312` | `WorldCurrentAndStagedMatrices` | Current and staged signed 8.8 Mode-7 A/B/C/D transforms. |
| `$7E:0314/$0316/$0318` | `WorldRotation/ZoomCurrent/ZoomTarget` | World-navigation rotation and zoom state, including the act-entry spin/zoom. |
| `$7E:0341` | `WorldLocation` | 1-based location-label/destination rectangle selected by `$01:B6CA`; zero outside all borders. |
| `$7E:0380-$059F` | `OamShadow` | 512-byte low table plus 32-byte high table. |
| `$7E:06A0+` | `ActionObjectTable` / `SimFixedRecords` | Mode-dependent resident object/record array. **80** stride-`$40` slots (`$06A0`-`$1AA0`; allocator bound in `$853D`), not 24. Key fields: `+$12` handler, `+$1E` yield-resume, `+$24` wait counter, `+$3E` stashed return frame. |
| `$7E:08A0+` | `PlayerObject` | Action player slot inside the action-object table. |
| `$7E:08D0` | `PlayerStatusFlags` | Player status word: `$2000` = invulnerable, `$0008` = hurt; the magic cast gate vetoes on `BIT #$2008`. |
| `$7E:00A0` | `HeldButtons` | NMI-latched `$4218 & $F4` held-button byte (`#$00C0` = A or X = cast). |
| `$7E:0021` | `WorkingMp` | Act-mode working MP/scroll count; loaded from persistent MP at act entry. |
| `$7E:0295` | `PersistentMp` | Persistent MP/scroll count (save-backed); written only via long addressing by reward grants. |
| `$7E:02AC` | `SelectedMagic` | Equipped magic id chosen in the equip menu; zero blocks casting. |
| `$7E:0336` | `TitleMenuSelection` | Continue/new-game/professional selection used by `$02:A622`; observed values 0–2 in the interactive title state. |
| `$7E:0334` | `CurrentSongId` | Selected/requested song id; values observed during Death Heim transitions identify music selections, not a distinct ending-state flag or proof an upload has completed. |
| `$7E:0347` | `DeathHeimProgress` | Boss-rush progress (`$19 - 1` after each boss); `7` = all bosses beaten. |
| `$7E:035A/$035B` | `CopEventRequest` / `BrkSfxRequest` | Software-interrupt request ports (music/event id, SFX id). |
| `$7F:6B18` | `RegionCompletionFlags` | Six-region completion bitset checked by the post-Death-Heim return stager. |
| `$7E:6000-$7FFF` | `ActionGraphicsRasterWorkspace` | Shared command-7/5 decompression workspace and R1-R10 raster-table storage; retained decompression bytes can be presentation-visible. |
| `$7E:8000+/$C000+` | `ActionBg1Map/ActionBg2Map` | Active page-major action map prefixes. Sizes derive from the command-4 chunk dimensions; `$C000-$FFFF` is world/town scratch outside action mode. |
| `$7E:C000-$FFFF` | `WorldMapScratch` | Shared scratch. It is a world-map shadow only during `$19=09` build/presentation; action BG2 command 4 overwrites only its active prefix and towns reuse rows 0-7, so handwritten rendering must not treat it as persistent state. |
| `$7F:0000-$1FFF` | `TownBg1Tilemap` | Live quadrant-paged 64x64-tile town image. Each 32x32 town cell owns four words at `+$00/+$02/+$40/+$42`; this is the displayed-art authority during staged changes. |
| `$7E:2100-$28FF` | `ActionBg1Definitions` / `TownTerrainMetatileDefinitions` | Mode-dependent 2 KiB metatile table: action BG1 rendering/collision definitions or the town terrain atlas consumed by `$03:9B5A`. |
| `$7E:2900-$30FF` | `ActionBg2Definitions` | Action BG2's 2 KiB metatile table; shared scratch outside action mode. |
| `$7E:3100-$38FF` | `TownStructureMetatileDefinitions` | Loaded structure atlas, 256 entries of four tilemap words, consumed by `$03:9C43`. |
| `$7F:2000-$37FF` | `TownTerrainCellMaps` | Six quadrant-paged 32x32 semantic town maps consumed by traversal, reconstruction, and the developed-world composer. They are not the displayed-art authority during staged changes; use the live 2x2 words at `$7F:0000-$1FFF` for that. |
| `$7F:9101` | `WorldStateFlags` | Bit 0 controls the builder's 8x8 clear at tilemap offset `$0660`. |
| `$7E:0A00+` | `SimWorldRecords` | 44 stride-38 simulation world records. |
| `$7E:0AEE/$0AF0` | `SimCameraTargetX/Y` | Town camera-follow target. |
| `$7E:3900/$3A02` | `Bg1ColumnRecord` / `Bg1RowRecord` | Fixed BG1 tile-upload records. |
| `$7E:3B04/$3C06` | `Bg2ColumnRecord` / `Bg2RowRecord` | Fixed BG2 tile-upload records. |
| `$7F:B000+` | `HudBg3TilemapBuffer` | HUD/dialog BG3 tilemap staging. |

## How to extend this map

For each newly understood routine or table:

1. Capture the immutable address and all known callers/callees.
2. Describe inputs, outputs, state writes, width/DB assumptions, and return
   convention in the subsystem document.
3. Add or update one row here with the most conservative useful name.
4. Increase confidence only when the evidence supports it; note unresolved
   parts explicitly.
5. During the final stabilization pass, copy only **Verified** candidates into
   `recomp/bank*.cfg`, regenerate once, and review the resulting symbol-only
   diff independently from gameplay changes.

This map should grow whenever decompilation work answers a question, even when
the immediate recomp fix needs no new hook. That is how one investigation
prevents the next from retracing the same code.
